<UserControl
    x:Class="Assetra.WPF.Features.Portfolio.Controls.AddRecordDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="clr-namespace:Assetra.WPF.Infrastructure.Behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ds="clr-namespace:Assetra.WPF.DesignSystem.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:txforms="clr-namespace:Assetra.WPF.Features.Portfolio.Controls.TxForms"
    xmlns:vm="clr-namespace:Assetra.WPF.Features.Portfolio.SubViewModels"
    x:Name="Root"
    d:DataContext="{d:DesignInstance Type={x:Type vm:TransactionDialogViewModel}}"
    Visibility="{Binding IsTxDialogOpen, Converter={StaticResource BooleanToVisibilityConverter}}"
    mc:Ignorable="d">

    <Grid>
        <!--  半透明背景幕  -->
        <Grid Style="{StaticResource AppDialogOverlay}" />

        <!--  Dialog card：固定舒適閱讀寬度 + 內容滾動。  -->
        <!--  寬度跟隨視窗易讓表單欄位過寬難讀；採固定 600px 配合 ScrollViewer 高度上限。  -->
        <Border
            Width="600"
            MaxHeight="{StaticResource DialogMaxHeight}"
            Padding="0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            behaviors:EscapeKeyCommandBehavior.Command="{Binding CloseTxDialogCommand}"
            Style="{StaticResource AppDialogShell}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <!--  Header  -->
                    <RowDefinition Height="*" />
                    <!--  Content (scroll)  -->
                    <RowDefinition Height="Auto" />
                    <!--  Footer  -->
                </Grid.RowDefinitions>

                <!--  Header  -->
                <Border
                    Grid.Row="0"
                    Padding="20,14"
                    BorderBrush="{DynamicResource AppBorder}"
                    BorderThickness="0,0,0,1">
                    <DockPanel>
                        <StackPanel>
                            <TextBlock
                                VerticalAlignment="Center"
                                FontSize="{StaticResource Font.Size.Sm}"
                                FontWeight="SemiBold"
                                Foreground="{DynamicResource AppTextPrimary}">
                                <TextBlock.Style>
                                    <!--
                                        Title precedence (top wins):
                                        1. Replace prompt / Revision / Edit mode → fixed string
                                        2. Create mode → 動態依 TxDynamicTitleKey 切「新增買入交易」等
                                    -->
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="{Binding TxDynamicTitleKey, Converter={StaticResource ResourceKeyToStringConverter}}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsRevisionReplacePromptOpen}" Value="True">
                                                <Setter Property="Text" Value="{DynamicResource Portfolio.Record.ReplacePromptTitle}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsRevisionMode}" Value="True">
                                                <Setter Property="Text" Value="{DynamicResource Portfolio.Record.ReviseTitle}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                                                <Setter Property="Text" Value="{DynamicResource Portfolio.Tx.EditTitle}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <TextBlock
                                Margin="0,2,0,0"
                                FontSize="{StaticResource Font.Size.Xs}"
                                Foreground="{DynamicResource AppTextMuted}"
                                TextWrapping="Wrap">
                                <TextBlock.Style>
                                    <!--
                                        Subtitle: only meta-only edits and revision /
                                        replace flows show subtitle text. Direct-edit
                                        types skip it because their form fields make
                                        editability self-evident.
                                    -->
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsEditingMetaOnly}" Value="True">
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Setter Property="Text" Value="{DynamicResource Portfolio.Record.EditSubtitle}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsRevisionMode}" Value="True">
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Setter Property="Text" Value="{DynamicResource Portfolio.Record.RevisionSubtitle}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsRevisionReplacePromptOpen}" Value="True">
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Setter Property="Text" Value="{DynamicResource Portfolio.Record.ReplaceSubtitle}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                        <Button
                            Width="28"
                            Height="28"
                            Padding="0"
                            HorizontalAlignment="Right"
                            Command="{Binding CloseTxDialogCommand}"
                            DockPanel.Dock="Right"
                            Style="{StaticResource AppIconButton}"
                            ToolTip="{DynamicResource Common.Close}">
                            <ds:AppIcon
                                FontSize="12"
                                Foreground="{DynamicResource AppTextMuted}"
                                Symbol="Dismiss24" />
                        </Button>
                    </DockPanel>
                </Border>

                <!--  Scrollable content  -->
                <ScrollViewer
                    x:Name="ContentScrollViewer"
                    Grid.Row="1"
                    Padding="20,16"
                    VerticalScrollBarVisibility="Auto">
                    <StackPanel>

                        <!--
                            Compact meta-only edit banner. Replaces the previous trio of
                            stacked banners (locked summary card + ReviseHint + MetaOnlyHint)
                            with a single inline lock-card. Shown ONLY for meta-only edits
                            (Sell / Transfer / legacy Buy/StockDiv); direct-editable types
                            see no banner — their fields are pre-filled and editable inline.
                            
                            Layout: 🔒 icon (left) | summary line + lock reason (right)
                        -->
                        <Border
                            Margin="0,0,0,12"
                            Padding="12,10"
                            Background="{DynamicResource AppSurfaceAlt}"
                            BorderBrush="{DynamicResource AppBorder}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource Radius.Sm}"
                            Visibility="{Binding ShowEditLockedSummary, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <ds:AppIcon
                                    Grid.Column="0"
                                    Margin="0,1,10,0"
                                    VerticalAlignment="Top"
                                    FontSize="14"
                                    Foreground="{DynamicResource AppTextMuted}"
                                    Symbol="LockClosed24" />
                                <StackPanel Grid.Column="1">
                                    <!--  Summary line: 類型 · 對象 · 金額 × 數量  -->
                                    <TextBlock
                                        FontSize="{StaticResource Font.Size.Sm}"
                                        FontWeight="SemiBold"
                                        Foreground="{DynamicResource AppTextPrimary}"
                                        TextWrapping="Wrap">
                                        <Run Text="{Binding EditSummaryType, Mode=OneWay}" />
                                        <Run Foreground="{DynamicResource AppTextMuted}" Text=" · " />
                                        <Run Text="{Binding EditSummaryTarget, Mode=OneWay}" />
                                    </TextBlock>
                                    <TextBlock
                                        Margin="0,2,0,0"
                                        FontFamily="{StaticResource Font.Family.Base}"
                                        FontSize="{StaticResource Font.Size.Xs}"
                                        Foreground="{DynamicResource AppTextSecondary}"
                                        Typography.NumeralAlignment="Tabular">
                                        <Run Text="{Binding EditSummaryAmount, Mode=OneWay}" />
                                        <Run Foreground="{DynamicResource AppTextMuted}" Text=" × " />
                                        <Run Text="{Binding EditSummaryQuantity, Mode=OneWay}" />
                                    </TextBlock>
                                    <!--  Lock reason — single line, replaces CoreLocked + MetaOnlyHint  -->
                                    <TextBlock
                                        Margin="0,8,0,0"
                                        FontSize="{StaticResource Font.Size.Xs}"
                                        Foreground="{DynamicResource AppTextSecondary}"
                                        Text="{DynamicResource Portfolio.Record.CoreLocked}"
                                        TextWrapping="Wrap" />
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!--
                            Revision-mode hint — kept only for the legacy "建立修正版"
                            (now "刪除此筆並新增") flow on meta-only edits, where the user
                            explicitly clicked the recreate button.
                        -->
                        <Border
                            Margin="0,0,0,12"
                            Padding="10,8"
                            Background="{DynamicResource AppBackground}"
                            BorderBrush="{DynamicResource AppBorder}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource Radius.Sm}"
                            Visibility="{Binding IsRevisionMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock
                                FontSize="{StaticResource Font.Size.Xs}"
                                Foreground="{DynamicResource AppTextSecondary}"
                                Text="{DynamicResource Portfolio.Record.RevisionModeHint}"
                                TextWrapping="Wrap" />
                        </Border>

                        <StackPanel Visibility="{Binding IsRevisionReplacePromptOpen, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                            <Border
                                Margin="0,0,0,12"
                                Padding="14,12"
                                Background="{DynamicResource AppBackground}"
                                BorderBrush="{DynamicResource AppBorder}"
                                BorderThickness="1"
                                CornerRadius="{StaticResource Radius.Sm}">
                                <DockPanel LastChildFill="True">
                                    <TextBlock
                                        DockPanel.Dock="Top"
                                        FontSize="{StaticResource Font.Size.Sm}"
                                        FontWeight="SemiBold"
                                        Foreground="{DynamicResource AppTextPrimary}"
                                        Text="{DynamicResource Portfolio.Record.Section.Basic}" />
                                    <TextBlock
                                        Margin="0,4,0,12"
                                        DockPanel.Dock="Top"
                                        FontSize="{StaticResource Font.Size.Xs}"
                                        Foreground="{DynamicResource AppTextMuted}"
                                        Text="{DynamicResource Portfolio.Record.Section.BasicHint}"
                                        TextWrapping="Wrap"
                                        Visibility="{Binding ShowEditLockedSummary, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                                    <Border
                                        Width="172"
                                        Margin="0,0,12,0"
                                        Padding="0"
                                        Background="Transparent"
                                        DockPanel.Dock="Left">
                                        <StackPanel>
                                            <TextBlock
                                                Margin="0,0,0,4"
                                                FontSize="{StaticResource Font.Size.Xs}"
                                                FontWeight="SemiBold"
                                                Foreground="{DynamicResource AppTextSecondary}"
                                                Text="{DynamicResource Portfolio.Tx.Date}" />
                                            <DatePicker
                                                MinWidth="132"
                                                HorizontalContentAlignment="Left"
                                                SelectedDate="{Binding TxDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                Style="{StaticResource AppDatePicker}" />
                                        </StackPanel>
                                    </Border>

                                    <!--
                                        Type picker — shown for new records and direct-edit
                                        types (Income / CashDividend / Buy w/ entry / etc.).
                                        Hidden ONLY for meta-only edits where changing type
                                        would be meaningless (the locked summary already
                                        states the immutable type).
                                    -->
                                    <StackPanel Visibility="{Binding ShowEditLockedSummary, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                                        <TextBlock
                                            Margin="0,0,0,4"
                                            FontSize="{StaticResource Font.Size.Xs}"
                                            FontWeight="SemiBold"
                                            Foreground="{DynamicResource AppTextSecondary}"
                                            Text="{DynamicResource Portfolio.TradeHistory.Type}" />
                                        <Border Padding="10" Style="{StaticResource FormCard}">
                                            <StackPanel>
                                                <!--  1. 投資交易（買入 / 賣出 / 現金股利 / 股票股利）— Income 已搬出  -->
                                                <Border
                                                    Padding="8"
                                                    Background="Transparent"
                                                    BorderBrush="{DynamicResource AppBorder}"
                                                    BorderThickness="1"
                                                    CornerRadius="{StaticResource Radius.Sm}">
                                                    <StackPanel>
                                                        <TextBlock
                                                            FontSize="{StaticResource Font.Size.Xs}"
                                                            FontWeight="SemiBold"
                                                            Foreground="{DynamicResource AppTextMuted}"
                                                            Text="{DynamicResource Portfolio.Record.Group.Investment}" />
                                                        <WrapPanel Margin="0,6,0,0">
                                                            <RadioButton
                                                                Content="{DynamicResource Portfolio.Tx.Buy}"
                                                                GroupName="TxType"
                                                                IsChecked="{Binding TxType, Converter={StaticResource EqualityConverter}, ConverterParameter=buy, Mode=TwoWay}"
                                                                Style="{StaticResource SegmentBtn}" />
                                                            <RadioButton
                                                                Content="{DynamicResource Portfolio.Tx.Sell}"
                                                                GroupName="TxType"
                                                                IsChecked="{Binding TxType, Converter={StaticResource EqualityConverter}, ConverterParameter=sell, Mode=TwoWay}"
                                                                Style="{StaticResource SegmentBtn}" />
                                                            <RadioButton
                                                                Content="{DynamicResource Portfolio.Tx.CashDiv}"
                                                                GroupName="TxType"
                                                                IsChecked="{Binding TxType, Converter={StaticResource EqualityConverter}, ConverterParameter=cashDiv, Mode=TwoWay}"
                                                                Style="{StaticResource SegmentBtn}" />
                                                            <RadioButton
                                                                Content="{DynamicResource Portfolio.Tx.StockDiv}"
                                                                GroupName="TxType"
                                                                IsChecked="{Binding TxType, Converter={StaticResource EqualityConverter}, ConverterParameter=stockDiv, Mode=TwoWay}"
                                                                Style="{StaticResource SegmentBtn}" />
                                                        </WrapPanel>
                                                    </StackPanel>
                                                </Border>

                                                <!--  2. 收入與支出（薪資 / 一般收入；提款也是支出但走資金帳戶 group）  -->
                                                <Border
                                                    Margin="0,6,0,0"
                                                    Padding="8"
                                                    Background="Transparent"
                                                    BorderBrush="{DynamicResource AppBorder}"
                                                    BorderThickness="1"
                                                    CornerRadius="{StaticResource Radius.Sm}">
                                                    <StackPanel>
                                                        <TextBlock
                                                            FontSize="{StaticResource Font.Size.Xs}"
                                                            FontWeight="SemiBold"
                                                            Foreground="{DynamicResource AppTextMuted}"
                                                            Text="{DynamicResource Portfolio.Record.Group.IncomeExpense}" />
                                                        <WrapPanel Margin="0,6,0,0">
                                                            <RadioButton
                                                                Content="{DynamicResource Portfolio.Tx.Income}"
                                                                GroupName="TxType"
                                                                IsChecked="{Binding TxType, Converter={StaticResource EqualityConverter}, ConverterParameter=income, Mode=TwoWay}"
                                                                Style="{StaticResource SegmentBtn}" />
                                                        </WrapPanel>
                                                    </StackPanel>
                                                </Border>

                                                <!--  3. 資金帳戶（存入 / 提款 / 轉帳）  -->
                                                <Border
                                                    Margin="0,6,0,0"
                                                    Padding="8"
                                                    Background="Transparent"
                                                    BorderBrush="{DynamicResource AppBorder}"
                                                    BorderThickness="1"
                                                    CornerRadius="{StaticResource Radius.Sm}">
                                                    <StackPanel>
                                                        <TextBlock
                                                            FontSize="{StaticResource Font.Size.Xs}"
                                                            FontWeight="SemiBold"
                                                            Foreground="{DynamicResource AppTextMuted}"
                                                            Text="{DynamicResource Portfolio.Record.Group.Cash}" />
                                                        <WrapPanel Margin="0,6,0,0">
                                                            <RadioButton
                                                                Content="{DynamicResource Portfolio.TradeType.Deposit}"
                                                                GroupName="TxType"
                                                                IsChecked="{Binding TxType, Converter={StaticResource EqualityConverter}, ConverterParameter=deposit, Mode=TwoWay}"
                                                                Style="{StaticResource SegmentBtn}"
                                                                ToolTip="{DynamicResource Portfolio.Tx.DepositHint}" />
                                                            <RadioButton
                                                                Content="{DynamicResource Portfolio.Tx.WithdrawalAction}"
                                                                GroupName="TxType"
                                                                IsChecked="{Binding TxType, Converter={StaticResource EqualityConverter}, ConverterParameter=withdrawal, Mode=TwoWay}"
                                                                Style="{StaticResource SegmentBtn}"
                                                                ToolTip="{DynamicResource Portfolio.Tx.WithdrawalHint}" />
                                                            <RadioButton
                                                                Content="{DynamicResource Portfolio.Tx.TransferAction}"
                                                                GroupName="TxType"
                                                                IsChecked="{Binding TxType, Converter={StaticResource EqualityConverter}, ConverterParameter=transfer, Mode=TwoWay}"
                                                                Style="{StaticResource SegmentBtn}"
                                                                ToolTip="{DynamicResource Portfolio.Tx.TransferHint}" />
                                                        </WrapPanel>
                                                    </StackPanel>
                                                </Border>

                                                <Border
                                                    Margin="0,6,0,0"
                                                    Padding="8"
                                                    Background="Transparent"
                                                    BorderBrush="{DynamicResource AppBorder}"
                                                    BorderThickness="1"
                                                    CornerRadius="{StaticResource Radius.Sm}">
                                                    <StackPanel>
                                                        <TextBlock
                                                            FontSize="{StaticResource Font.Size.Xs}"
                                                            FontWeight="SemiBold"
                                                            Foreground="{DynamicResource AppTextMuted}"
                                                            Text="{DynamicResource Portfolio.Record.Group.Liability}" />
                                                        <WrapPanel Margin="0,6,0,0">
                                                            <RadioButton
                                                                Content="{DynamicResource Portfolio.TradeType.LoanBorrow}"
                                                                GroupName="TxType"
                                                                IsChecked="{Binding TxType, Converter={StaticResource EqualityConverter}, ConverterParameter=loanBorrow, Mode=TwoWay}"
                                                                Style="{StaticResource SegmentBtn}" />
                                                            <RadioButton
                                                                Content="{DynamicResource Portfolio.TradeType.LoanRepay}"
                                                                GroupName="TxType"
                                                                IsChecked="{Binding TxType, Converter={StaticResource EqualityConverter}, ConverterParameter=loanRepay, Mode=TwoWay}"
                                                                Style="{StaticResource SegmentBtn}" />
                                                            <RadioButton
                                                                Content="{DynamicResource Portfolio.TradeType.CreditCardCharge}"
                                                                GroupName="TxType"
                                                                IsChecked="{Binding TxType, Converter={StaticResource EqualityConverter}, ConverterParameter=creditCardCharge, Mode=TwoWay}"
                                                                Style="{StaticResource SegmentBtn}" />
                                                            <RadioButton
                                                                Content="{DynamicResource Portfolio.TradeType.CreditCardPayment}"
                                                                GroupName="TxType"
                                                                IsChecked="{Binding TxType, Converter={StaticResource EqualityConverter}, ConverterParameter=creditCardPayment, Mode=TwoWay}"
                                                                Style="{StaticResource SegmentBtn}" />
                                                        </WrapPanel>
                                                    </StackPanel>
                                                </Border>

                                                <!--
                                                    顯示位置提示 — 選類型後告訴使用者這筆交易會出現在哪幾頁。
                                                    解決「新增的資料去哪了」first-time 困惑。
                                                    HasTxDestination 為 false（未選類型）時隱藏。
                                                -->
                                                <Border
                                                    Margin="0,8,0,0"
                                                    Padding="10,8"
                                                    Background="{DynamicResource AppAccentSubtle}"
                                                    BorderBrush="{DynamicResource AppAccent}"
                                                    BorderThickness="1"
                                                    CornerRadius="{StaticResource Radius.Sm}"
                                                    Visibility="{Binding HasTxDestination, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                    <StackPanel Orientation="Horizontal">
                                                        <ds:AppIcon
                                                            Margin="0,1,8,0"
                                                            VerticalAlignment="Top"
                                                            FontSize="12"
                                                            Foreground="{DynamicResource AppAccent}"
                                                            Symbol="Info24" />
                                                        <StackPanel>
                                                            <TextBlock
                                                                FontSize="{StaticResource Font.Size.Xs}"
                                                                Foreground="{DynamicResource AppTextSecondary}"
                                                                Text="{DynamicResource Portfolio.Record.Destination.Hint}" />
                                                            <TextBlock
                                                                Margin="0,2,0,0"
                                                                FontSize="{StaticResource Font.Size.Xs}"
                                                                FontWeight="SemiBold"
                                                                Foreground="{DynamicResource AppAccent}"
                                                                Text="{Binding TxDestinationKey, Converter={StaticResource ResourceKeyToStringConverter}}"
                                                                TextWrapping="Wrap" />
                                                        </StackPanel>
                                                    </StackPanel>
                                                </Border>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </DockPanel>
                            </Border>

                            <Border
                                Padding="14,12"
                                Background="{DynamicResource AppBackground}"
                                BorderBrush="{DynamicResource AppBorder}"
                                BorderThickness="1"
                                CornerRadius="{StaticResource Radius.Sm}">
                                <StackPanel>
                                    <TextBlock
                                        FontSize="{StaticResource Font.Size.Sm}"
                                        FontWeight="SemiBold"
                                        Foreground="{DynamicResource AppTextPrimary}"
                                        Text="{DynamicResource Portfolio.Record.Section.Details}" />
                                    <TextBlock
                                        Margin="0,4,0,12"
                                        FontSize="{StaticResource Font.Size.Xs}"
                                        Foreground="{DynamicResource AppTextMuted}"
                                        Text="{DynamicResource Portfolio.Record.Section.DetailsHint}"
                                        TextWrapping="Wrap"
                                        Visibility="{Binding ShowEditLockedSummary, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />

                                    <!--
                                        Per-type form panel — shown for new records and
                                        direct-edit types so users can change economic
                                        fields. Hidden ONLY for meta-only edits where the
                                        compact lock card + note-only fallback below
                                        replaces it.
                                    -->
                                    <StackPanel Visibility="{Binding ShowEditLockedSummary, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                                        <Border Height="2" Background="Transparent" />
                                        <!--  ======== 買入欄位 ========  -->
                                        <txforms:BuyTxForm />

                                        <!--  ======== 賣出欄位 ========  -->
                                        <txforms:SellTxForm />

                                        <!--  ======== 收入欄位 ========  -->
                                        <txforms:IncomeTxForm />

                                        <!--  ======== 現金股利欄位 ========  -->
                                        <txforms:CashDividendTxForm />

                                        <!--  ======== 股票股利欄位 ========  -->
                                        <txforms:StockDividendTxForm />

                                        <!--  ======== 存入/提款欄位 ========  -->
                                        <txforms:CashFlowTxForm />

                                        <!--  ======== 借款/還款欄位 ========  -->
                                        <txforms:LoanTxForm />

                                        <!--  ======== 信用卡欄位 ========  -->
                                        <txforms:CreditCardTxForm />

                                        <!--  ======== 轉帳欄位 ========  -->
                                        <txforms:TransferTxForm />
                                    </StackPanel>

                                    <!--
                                        Impact preview — projects the post-save balance of
                                        the selected cash account so the user can sanity-check
                                        before clicking Save. Computed in
                                        TransactionDialogViewModel.Preview.cs; visible only
                                        when HasImpactPreview is true (i.e. supported type
                                        + parseable amount + balance actually changes).
                                        Hidden for meta-only edits since the form is hidden.
                                    -->
                                    <Border
                                        Margin="0,12,0,0"
                                        Padding="10,8"
                                        Background="{DynamicResource AppAccentSubtle}"
                                        BorderBrush="{DynamicResource AppAccent}"
                                        BorderThickness="3,0,0,0"
                                        CornerRadius="{StaticResource Radius.Sm}"
                                        Visibility="{Binding HasImpactPreview, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <StackPanel Orientation="Horizontal">
                                            <ds:AppIcon
                                                Margin="0,1,8,0"
                                                VerticalAlignment="Top"
                                                FontSize="13"
                                                Foreground="{DynamicResource AppAccent}"
                                                Symbol="DataPie24" />
                                            <TextBlock
                                                FontFamily="{StaticResource Font.Family.Base}"
                                                FontSize="{StaticResource Font.Size.Xs}"
                                                Foreground="{DynamicResource AppTextPrimary}"
                                                Text="{Binding ImpactPreviewText}"
                                                TextWrapping="Wrap"
                                                Typography.NumeralAlignment="Tabular" />
                                        </StackPanel>
                                    </Border>

                                    <StackPanel Visibility="{Binding ShowEditLockedSummary, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock
                                            Margin="0,0,0,4"
                                            FontSize="{StaticResource Font.Size.Xs}"
                                            FontWeight="SemiBold"
                                            Foreground="{DynamicResource AppTextSecondary}"
                                            Text="{DynamicResource Portfolio.Tx.Note}" />
                                        <TextBox Style="{StaticResource AppTextBox}" Text="{Binding TxNote, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>

                                    <!--  Error  -->
                                    <TextBlock
                                        Margin="0,12,0,0"
                                        FontSize="{StaticResource Font.Size.Xs}"
                                        Foreground="{DynamicResource AppDown}"
                                        Text="{Binding TxError}"
                                        Visibility="{Binding TxError, Converter={StaticResource StringToVisibilityConverter}}" />
                                </StackPanel>
                            </Border>
                        </StackPanel>

                        <StackPanel Visibility="{Binding IsRevisionReplacePromptOpen, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Border
                                Margin="0,0,0,12"
                                Padding="12"
                                Background="{DynamicResource AppBackground}"
                                BorderBrush="{DynamicResource AppBorder}"
                                BorderThickness="1"
                                CornerRadius="{StaticResource Radius.Sm}">
                                <StackPanel>
                                    <TextBlock
                                        Margin="0,0,0,8"
                                        FontSize="{StaticResource Font.Size.Sm}"
                                        FontWeight="SemiBold"
                                        Foreground="{DynamicResource AppTextPrimary}"
                                        Text="{DynamicResource Portfolio.Record.ReplacePromptTitle}" />
                                    <TextBlock
                                        FontSize="{StaticResource Font.Size.Xs}"
                                        Foreground="{DynamicResource AppTextSecondary}"
                                        Text="{DynamicResource Portfolio.Record.ReplacePrompt}"
                                        TextWrapping="Wrap" />
                                </StackPanel>
                            </Border>
                            <TextBlock
                                Margin="0,0,0,12"
                                FontSize="{StaticResource Font.Size.Xs}"
                                Foreground="{DynamicResource AppDown}"
                                Text="{Binding RevisionReplacePromptError}"
                                Visibility="{Binding RevisionReplacePromptError, Converter={StaticResource StringToVisibilityConverter}}" />
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>

                <!--  Footer  -->
                <Border
                    Grid.Row="2"
                    Padding="20,12"
                    BorderBrush="{DynamicResource AppBorder}"
                    BorderThickness="0,1,0,0">
                    <Grid>
                        <Grid>
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Value="True">
                                            <DataTrigger.Binding>
                                                <MultiBinding Converter="{StaticResource WidthScaleThresholdToBooleanConverter}" ConverterParameter="980">
                                                    <Binding ElementName="Root" Path="ActualWidth" />
                                                    <Binding Path="DataContext.Settings.UiScale" RelativeSource="{RelativeSource AncestorType=Window}" />
                                                </MultiBinding>
                                            </DataTrigger.Binding>
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>
                            <DockPanel>
                                <!--  刪除按鈕（僅編輯模式）  -->
                                <Button
                                    Height="30"
                                    Padding="14,0"
                                    Command="{Binding RequestDeleteTradeCommand}"
                                    DockPanel.Dock="Left"
                                    Style="{StaticResource AppDangerButton}"
                                    Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <TextBlock Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{DynamicResource Portfolio.Tx.Delete}" />
                                </Button>
                                <!--
                                    「刪除此筆並新增」只在 meta-only 編輯（Sell / Transfer /
                                    legacy Buy/StockDiv）時才有意義 — 其他類型直接按「儲存」
                                    就會自動覆寫原紀錄，不需要二次選擇。
                                -->
                                <Button
                                    Height="30"
                                    Margin="8,0,0,0"
                                    Padding="14,0"
                                    Command="{Binding CreateRevisionCommand}"
                                    DockPanel.Dock="Left"
                                    Style="{StaticResource AppSecondaryButton}"
                                    Visibility="{Binding IsEditingMetaOnly, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <TextBlock Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{DynamicResource Portfolio.Record.CreateRevision}" />
                                </Button>
                                <!--  取消 / 儲存（靠右）  -->
                                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                                    <StackPanel Orientation="Horizontal" Visibility="{Binding IsRevisionReplacePromptOpen, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <Button
                                            Height="30"
                                            Margin="0,0,8,0"
                                            Padding="14,0"
                                            Command="{Binding KeepBothRecordsCommand}"
                                            Style="{StaticResource AppSecondaryButton}">
                                            <TextBlock Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{DynamicResource Portfolio.Record.KeepBoth}" />
                                        </Button>
                                        <Button
                                            Height="30"
                                            Padding="14,0"
                                            Command="{Binding ReplaceOriginalRecordCommand}"
                                            Style="{StaticResource AppPrimaryButton}">
                                            <TextBlock Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{DynamicResource Portfolio.Record.ReplaceAndDelete}" />
                                        </Button>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Visibility="{Binding IsRevisionReplacePromptOpen, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                                        <Button
                                            Height="30"
                                            Margin="0,0,8,0"
                                            Padding="14,0"
                                            Command="{Binding CloseTxDialogCommand}"
                                            Style="{StaticResource AppSecondaryButton}">
                                            <TextBlock Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{DynamicResource Portfolio.Cancel}" />
                                        </Button>
                                        <Button
                                            Height="30"
                                            Padding="14,0"
                                            Command="{Binding ConfirmTxCommand}"
                                            Style="{StaticResource AppPrimaryButton}">
                                            <TextBlock>
                                                <TextBlock.Style>
                                                    <Style BasedOn="{StaticResource AppButtonText}" TargetType="TextBlock">
                                                        <Setter Property="Text" Value="{DynamicResource Portfolio.Tx.Confirm}" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                                                                <Setter Property="Text" Value="{DynamicResource Portfolio.Tx.Save}" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Button>
                                    </StackPanel>
                                </StackPanel>
                            </DockPanel>
                        </Grid>

                        <StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Value="True">
                                            <DataTrigger.Binding>
                                                <MultiBinding Converter="{StaticResource WidthScaleThresholdToBooleanConverter}" ConverterParameter="980">
                                                    <Binding ElementName="Root" Path="ActualWidth" />
                                                    <Binding Path="DataContext.Settings.UiScale" RelativeSource="{RelativeSource AncestorType=Window}" />
                                                </MultiBinding>
                                            </DataTrigger.Binding>
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <StackPanel Orientation="Horizontal" Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Button
                                    Height="30"
                                    Margin="0,0,8,8"
                                    Padding="14,0"
                                    Command="{Binding RequestDeleteTradeCommand}"
                                    Style="{StaticResource AppDangerButton}">
                                    <TextBlock Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{DynamicResource Portfolio.Tx.Delete}" />
                                </Button>
                                <!--  Same meta-only gate as the desktop layout above.  -->
                                <Button
                                    Height="30"
                                    Margin="0,0,0,8"
                                    Padding="14,0"
                                    Command="{Binding CreateRevisionCommand}"
                                    Style="{StaticResource AppSecondaryButton}"
                                    Visibility="{Binding IsEditingMetaOnly, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <TextBlock Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{DynamicResource Portfolio.Record.CreateRevision}" />
                                </Button>
                            </StackPanel>
                            <StackPanel Visibility="{Binding IsRevisionReplacePromptOpen, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Button
                                    Height="30"
                                    Margin="0,0,0,8"
                                    Padding="14,0"
                                    Command="{Binding ReplaceOriginalRecordCommand}"
                                    Style="{StaticResource AppPrimaryButton}">
                                    <TextBlock Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{DynamicResource Portfolio.Record.ReplaceAndDelete}" />
                                </Button>
                                <Button
                                    Height="30"
                                    Padding="14,0"
                                    Command="{Binding KeepBothRecordsCommand}"
                                    Style="{StaticResource AppSecondaryButton}">
                                    <TextBlock Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{DynamicResource Portfolio.Record.KeepBoth}" />
                                </Button>
                            </StackPanel>
                            <StackPanel
                                HorizontalAlignment="Right"
                                Orientation="Horizontal"
                                Visibility="{Binding IsRevisionReplacePromptOpen, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                                <Button
                                    Height="30"
                                    Margin="0,0,8,0"
                                    Padding="14,0"
                                    Command="{Binding CloseTxDialogCommand}"
                                    Style="{StaticResource AppSecondaryButton}">
                                    <TextBlock Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{DynamicResource Portfolio.Cancel}" />
                                </Button>
                                <Button
                                    Height="30"
                                    Padding="14,0"
                                    Command="{Binding ConfirmTxCommand}"
                                    Style="{StaticResource AppPrimaryButton}">
                                    <TextBlock>
                                        <TextBlock.Style>
                                            <Style BasedOn="{StaticResource AppButtonText}" TargetType="TextBlock">
                                                <Setter Property="Text" Value="{DynamicResource Portfolio.Tx.Confirm}" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                                                        <Setter Property="Text" Value="{DynamicResource Portfolio.Tx.Save}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <Grid
            Panel.ZIndex="200"
            Style="{StaticResource AppDialogOverlay}"
            Visibility="{Binding IsDeleteConfirmOpen, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <Border
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Panel.ZIndex="201"
            behaviors:EscapeKeyCommandBehavior.Command="{Binding CancelDeleteTradeCommand}"
            Style="{StaticResource AppDestructiveDialogShell}"
            Visibility="{Binding IsDeleteConfirmOpen, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Style="{StaticResource AppDialogTitle}" Text="{DynamicResource Common.DeleteConfirmTitle}" />
                <TextBlock Grid.Row="1" Style="{StaticResource AppDialogSubtitle}">
                    <Run Text="{DynamicResource Common.DeleteConfirmPrefix}" />
                    <Run Text="{Binding DeleteTargetName}" />
                    <Run Text="{DynamicResource Common.DeleteConfirmSuffix}" />
                </TextBlock>

                <StackPanel Grid.Row="2" Style="{StaticResource AppDialogFooter}">
                    <Button
                        Margin="0,0,8,0"
                        Command="{Binding CancelDeleteTradeCommand}"
                        Content="{DynamicResource Common.Cancel}"
                        Style="{StaticResource AppSecondaryButton}" />
                    <Button
                        Command="{Binding ConfirmDeleteTradeCommand}"
                        Content="{DynamicResource Common.Delete}"
                        Style="{StaticResource AppDangerButton}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
