<UserControl
    x:Class="Assetra.WPF.Features.Portfolio.Controls.TxForms.StockDividendTxForm"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:behaviors="clr-namespace:Assetra.WPF.Infrastructure.Behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Assetra.WPF.Features.Portfolio.SubViewModels"
    d:DataContext="{d:DesignInstance Type={x:Type vm:TransactionDialogViewModel},
                                     IsDesignTimeCreatable=False}"
    mc:Ignorable="d">

    <!--  ======== 股票股利欄位 ========  -->
    <StackPanel Visibility="{Binding TxTypeIsStockDiv, Converter={StaticResource BooleanToVisibilityConverter}}">
        <TextBlock
            Margin="0,0,0,4"
            FontSize="{StaticResource FontSizeXs}"
            FontWeight="SemiBold"
            Foreground="{DynamicResource AppTextSecondary}"
            Text="{DynamicResource Portfolio.Tx.Stock}" />
        <ComboBox
            Margin="0,0,0,12"
            IsEnabled="{Binding AreEconomicFieldsEditable}"
            ItemsSource="{Binding Positions}"
            SelectedItem="{Binding TxStockDivPosition, Mode=TwoWay}"
            Style="{StaticResource FormComboBox}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock
                            Margin="0,0,8,0"
                            FontFamily="{StaticResource FontBase}" Typography.NumeralAlignment="Tabular"
                            FontWeight="SemiBold"
                            Foreground="{DynamicResource AppAccent}"
                            Text="{Binding Symbol}" />
                        <TextBlock Foreground="{DynamicResource AppTextPrimary}" Text="{Binding Name}" />
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBlock
            Margin="0,0,0,4"
            FontSize="{StaticResource FontSizeXs}"
            FontWeight="SemiBold"
            Foreground="{DynamicResource AppTextSecondary}"
            Text="{DynamicResource Portfolio.Tx.NewShares}" />
        <ui:TextBox
            behaviors:ThousandSeparatorBehavior.IsEnabled="True"
            IsEnabled="{Binding AreEconomicFieldsEditable}"

            Text="{Binding TxStockDivNewShares, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock
            Style="{StaticResource FormFieldError}"
            Text="{Binding TxStockDivNewSharesError}"
            Visibility="{Binding TxStockDivNewSharesError, Converter={StaticResource StringToVisibilityConverter}}" />
    </StackPanel>
</UserControl>
