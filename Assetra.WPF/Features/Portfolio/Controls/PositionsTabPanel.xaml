<UserControl
    x:Class="Assetra.WPF.Features.Portfolio.Controls.PositionsTabPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Assetra.WPF.Controls"
    xmlns:conv="clr-namespace:Assetra.WPF.Infrastructure.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ds="clr-namespace:Assetra.WPF.DesignSystem.Controls"
    xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
    xmlns:lvcsk="clr-namespace:LiveChartsCore.SkiaSharpView;assembly=LiveChartsCore.SkiaSharpView"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:Assetra.Core.Models;assembly=Assetra.Core"
    xmlns:portfolio="clr-namespace:Assetra.WPF.Features.Portfolio.Controls"
    xmlns:vm="clr-namespace:Assetra.WPF.Features.Portfolio"
    x:Name="Root"
    d:DataContext="{d:DesignInstance Type={x:Type vm:PortfolioViewModel}}"
    mc:Ignorable="d">
    <UserControl.Resources>
        <conv:PercentToWidthConverter x:Key="PercentToWidthConverter" />

        <!--
            KPI 展開面板的關閉鈕 — 24×24 圓形、無框、hover 變淡背景。
            和 AppIconButton 的 32px 方框感分開，視覺更輕巧、配合面板頭部
            的小字標題等比例。
        -->
        <Style x:Key="PanelCloseButton" TargetType="Button">
            <Setter Property="Width" Value="24" />
            <Setter Property="Height" Value="24" />
            <Setter Property="MinHeight" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AppHover}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--
            Base chip 樣式 — 灰底圓角，hover 變色。每個 chip 在 inline 加上自己的
            DataTrigger 比對 AssetTypeFilter，命中時把 Background 切到 AppAccent。
            參考 frontend「pill / segmented control」典型樣式。
        -->
        <Style x:Key="AssetTypeFilterChipBase" TargetType="Button">
            <Setter Property="Padding" Value="14,5" />
            <Setter Property="MinHeight" Value="28" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Background" Value="{DynamicResource AppSurface}" />
            <Setter Property="Foreground" Value="{DynamicResource AppTextSecondary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource AppBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="{StaticResource Font.Size.Xs}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="13"
                            SnapsToDevicePixels="True">
                            <!--
                                直接用 TextBlock + TemplateBinding Foreground 取代 ContentPresenter +
                                TextElement.Foreground — 後者在某些全域 Button style 下 Foreground
                                不會傳到 auto-wrapped TextBlock，導致選中時白字仍渲染為深色文字 + 深藍底
                                的不可讀組合。
                            -->
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Foreground="{TemplateBinding Foreground}"
                                Text="{TemplateBinding Content}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AppHover}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!--
            Segmented toggle（iOS 風）— 持股佔比面板的「市值 / 成本」切換用。外層 Border 當 track，
            內部按鈕本身無框；選中時由 inline DataTrigger 填 AppAccent 成圓角 pill，未選為透明（露出 track）。
        -->
        <Style x:Key="SegmentedToggleButton" TargetType="Button">
            <Setter Property="MinHeight" Value="22" />
            <Setter Property="Padding" Value="14,3" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource AppTextSecondary}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="FontSize" Value="{StaticResource Font.Size.Xs}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="6"
                            SnapsToDevicePixels="True">
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontSize="{TemplateBinding FontSize}"
                                Foreground="{TemplateBinding Foreground}"
                                Text="{TemplateBinding Content}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!--  Row 0: PortfolioTabStrip (Task 1.6 — horizontal pill row of portfolios)  -->
            <RowDefinition Height="Auto" />
            <!--  Row 1: PortfolioDetailHeader (value / pnl / trend / ＋投資) — Task 1.4  -->
            <RowDefinition Height="Auto" />
            <!--  Row 2: PortfolioCompositionCard (股票 vs ETF focus card) — Task 1.5/1.6  -->
            <RowDefinition Height="Auto" />
            <!--  Row 3: Investment summary cards  -->
            <RowDefinition Height="Auto" />
            <!--  Row 4: KPI expand panel (持股佔比 donut / 每日損益 柱狀圖)  -->
            <RowDefinition Height="Auto" />
            <!--  Row 5: Quote provider notice  -->
            <RowDefinition Height="Auto" />
            <!--  Row 6: Text search + options  -->
            <RowDefinition Height="*" />
            <!--  Row 7: DataGrid (all asset types in one list; sort by Type column to group)  -->
            <RowDefinition Height="Auto" />
            <!--  Row 8: Filter summary footer  -->
        </Grid.RowDefinitions>

        <!--  ═══ Row 0: Portfolio tab strip (Task 1.6) ═══  -->
        <portfolio:PortfolioTabStrip Grid.Row="0" />

        <!--  ═══ Row 1: Per-portfolio detail header (Task 1.4) ═══  -->
        <portfolio:PortfolioDetailHeader Grid.Row="1" />

        <!--  ═══ Row 2: (移除) 投資組合焦點卡 — 與「持股佔比」重複，UX 精簡時拿掉；保留空 row 不重編號 ═══  -->

        <!--
            ═══ Row 3: Investment Summary ═══
            v3 redesign：跟 LiabilityTabPanel / AccountsTabPanel 對齊—
            外層 background 改 AppBackground (灰，跟下方 datagrid 同色)，
            內 cards 用 AppCardBackground (白)，padding 統一 Padding.Card token。
        -->
        <!--
            ═══ Row 3: 精簡 insights 切換列 ═══
            取代原本與上方標題卡重複的 4 張 KPI 卡（市值／損益／今日漲跌）。
            chip 沿用既有 ToggleKpiPanelCommand + 下方 Row 4 展開面板：
            點一下展開對應分析圖、再點一下收合。標題卡為唯一頭條數字來源。
        -->
        <StackPanel
            Grid.Row="3"
            Margin="12,18,12,0"
            Orientation="Horizontal"
            Visibility="{Binding IsOverviewExpanded, Converter={StaticResource BooleanToVisibilityConverter}}">

            <!--  持股佔比  -->
            <Button
                Command="{Binding ToggleKpiPanelCommand}"
                CommandParameter="marketvalue"
                Content="{DynamicResource Portfolio.Kpi.Chip.Allocation}">
                <Button.Style>
                    <Style BasedOn="{StaticResource AssetTypeFilterChipBase}" TargetType="Button">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsKpiMarketValueExpanded}" Value="True">
                                <Setter Property="Background" Value="{DynamicResource AppAccent}" />
                                <Setter Property="Foreground" Value="{DynamicResource AppTextOnAccent}" />
                                <Setter Property="BorderBrush" Value="{DynamicResource AppAccent}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <!--  （成本分布 chip 已併入「持股佔比」面板的 市值/成本 切換）  -->

            <!--  每日損益  -->
            <Button
                Command="{Binding ToggleKpiPanelCommand}"
                CommandParameter="daychange"
                Content="{DynamicResource Portfolio.Kpi.Chip.Daily}">
                <Button.Style>
                    <Style BasedOn="{StaticResource AssetTypeFilterChipBase}" TargetType="Button">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsKpiDayChangeExpanded}" Value="True">
                                <Setter Property="Background" Value="{DynamicResource AppAccent}" />
                                <Setter Property="Foreground" Value="{DynamicResource AppTextOnAccent}" />
                                <Setter Property="BorderBrush" Value="{DynamicResource AppAccent}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        <!--
            ═══ Row 4: KPI expand panel ═══
            點 KPI 卡右上小圖示後展開的圖表面板。三種狀態：
            - IsKpiMarketValueExpanded → 持股佔比 donut（沿用 AllocationMiniChart）
            - IsKpiDayChangeExpanded   → 過去 30 天每日 PnL 柱狀圖
            - 都未展開 → Visibility=Collapsed，row 高度為 0
            一次只能展開一個，由 ExpandedKpiPanel 單一 state 控制。
        -->
        <Border
            Grid.Row="4"
            Margin="12,10,12,0"
            Padding="14,12"
            Background="{DynamicResource AppSurface}"
            BorderBrush="{DynamicResource AppBorder}"
            BorderThickness="1"
            CornerRadius="{StaticResource Radius.Sm}"
            Visibility="{Binding IsAnyKpiPanelExpanded, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid>
                <!--  持股佔比 donut + legend  -->
                <Grid Visibility="{Binding IsKpiMarketValueExpanded, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <DockPanel
                        Grid.Row="0"
                        Margin="0,0,0,8"
                        LastChildFill="False">
                        <TextBlock
                            FontSize="{StaticResource Font.Size.Sm}"
                            FontWeight="SemiBold"
                            Foreground="{DynamicResource AppTextPrimary}"
                            Text="{DynamicResource Portfolio.Kpi.AllocationTitle}" />
                        <Button
                            Command="{Binding ToggleKpiPanelCommand}"
                            CommandParameter="marketvalue"
                            DockPanel.Dock="Right"
                            Style="{StaticResource PanelCloseButton}"
                            ToolTip="{DynamicResource Common.Close}">
                            <ds:AppIcon
                                FontSize="11"
                                Foreground="{DynamicResource AppTextMuted}"
                                Symbol="Dismiss24" />
                        </Button>
                        <!--  市值 / 成本 切換（iOS 風 segmented control；合併原「成本分布」chip）  -->
                        <Border
                            Margin="0,0,8,0"
                            Padding="2"
                            VerticalAlignment="Center"
                            Background="{DynamicResource AppBackground}"
                            BorderBrush="{DynamicResource AppBorder}"
                            BorderThickness="1"
                            CornerRadius="8"
                            DockPanel.Dock="Right">
                            <StackPanel Orientation="Horizontal">
                                <Button
                                    Command="{Binding SetAllocationModeCommand}"
                                    CommandParameter="mv"
                                    Content="{DynamicResource Portfolio.Kpi.Alloc.ByValue}">
                                    <Button.Style>
                                        <Style BasedOn="{StaticResource SegmentedToggleButton}" TargetType="Button">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsAllocationByMarketValue}" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource AppAccent}" />
                                                    <Setter Property="Foreground" Value="{DynamicResource AppTextOnAccent}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <Button
                                    Command="{Binding SetAllocationModeCommand}"
                                    CommandParameter="cost"
                                    Content="{DynamicResource Portfolio.Kpi.Alloc.ByCost}">
                                    <Button.Style>
                                        <Style BasedOn="{StaticResource SegmentedToggleButton}" TargetType="Button">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsAllocationByCost}" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource AppAccent}" />
                                                    <Setter Property="Foreground" Value="{DynamicResource AppTextOnAccent}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </Border>
                    </DockPanel>
                    <Grid Grid.Row="1" Height="160">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <lvc:PieChart
                            Grid.Column="0"
                            Width="{Binding ActualHeight, RelativeSource={RelativeSource Self}}"
                            Background="Transparent"
                            InitialRotation="-90"
                            LegendPosition="Hidden"
                            Series="{Binding ActiveAllocationSeries}"
                            TooltipPosition="Center"
                            TooltipTextSize="11" />
                        <ItemsControl
                            Grid.Column="1"
                            Margin="16,4,0,0"
                            VerticalAlignment="Center"
                            ItemsSource="{Binding ActiveAllocationSlices}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="10" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <Ellipse
                                            Grid.Column="0"
                                            Width="10"
                                            Height="10"
                                            VerticalAlignment="Center">
                                            <Ellipse.Fill>
                                                <SolidColorBrush Color="{Binding ColorHex}" />
                                            </Ellipse.Fill>
                                        </Ellipse>
                                        <TextBlock
                                            Grid.Column="1"
                                            Margin="8,0,12,0"
                                            VerticalAlignment="Center"
                                            FontSize="{StaticResource Font.Size.Sm}"
                                            Foreground="{DynamicResource AppTextSecondary}"
                                            Text="{Binding Label}" />
                                        <TextBlock
                                            Grid.Column="2"
                                            Margin="0,0,8,0"
                                            VerticalAlignment="Center"
                                            FontFamily="{StaticResource Font.Family.Base}"
                                            FontSize="{StaticResource Font.Size.Sm}"
                                            FontWeight="SemiBold"
                                            Foreground="{DynamicResource AppTextPrimary}"
                                            Text="{Binding Percent, StringFormat={}{0:F1}%}"
                                            Typography.NumeralAlignment="Tabular" />
                                        <TextBlock
                                            Grid.Column="3"
                                            VerticalAlignment="Center"
                                            FontFamily="{StaticResource Font.Family.Base}"
                                            FontSize="{StaticResource Font.Size.Xs}"
                                            Foreground="{DynamicResource AppTextMuted}"
                                            Text="{Binding Value, StringFormat='NT\${0:N0}'}"
                                            Typography.NumeralAlignment="Tabular" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Grid>

                <!--  (移除) 付出成本分布獨立面板 — 已併入上方「持股佔比」面板的 市值/成本 切換  -->

                <!--  (移除) 盈虧檔數 pie — UX 精簡；資訊改由 footer「獲利/虧損」呈現  -->

                <!--  每日 PnL 柱狀圖（7/30/90 天可切換）  -->
                <Grid Visibility="{Binding IsKpiDayChangeExpanded, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <DockPanel
                        Grid.Row="0"
                        Margin="0,0,0,8"
                        LastChildFill="False">
                        <TextBlock
                            FontSize="{StaticResource Font.Size.Sm}"
                            FontWeight="SemiBold"
                            Foreground="{DynamicResource AppTextPrimary}"
                            Text="{DynamicResource Portfolio.Kpi.DailyPnlTitle}" />
                        <Button
                            Command="{Binding ToggleKpiPanelCommand}"
                            CommandParameter="daychange"
                            DockPanel.Dock="Right"
                            Style="{StaticResource PanelCloseButton}"
                            ToolTip="{DynamicResource Common.Close}">
                            <ds:AppIcon
                                FontSize="11"
                                Foreground="{DynamicResource AppTextMuted}"
                                Symbol="Dismiss24" />
                        </Button>
                    </DockPanel>
                    <!--  週期切換 chip row：7 / 30 / 90 天  -->
                    <StackPanel
                        Grid.Row="1"
                        Margin="0,0,0,6"
                        Orientation="Horizontal">
                        <Button
                            Command="{Binding SetDailyPnlPeriodCommand}"
                            CommandParameter="7"
                            Content="7 天">
                            <Button.Style>
                                <Style BasedOn="{StaticResource AssetTypeFilterChipBase}" TargetType="Button">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsPeriod7}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource AppAccent}" />
                                            <Setter Property="Foreground" Value="White" />
                                            <Setter Property="BorderBrush" Value="{DynamicResource AppAccent}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button
                            Command="{Binding SetDailyPnlPeriodCommand}"
                            CommandParameter="30"
                            Content="30 天">
                            <Button.Style>
                                <Style BasedOn="{StaticResource AssetTypeFilterChipBase}" TargetType="Button">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsPeriod30}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource AppAccent}" />
                                            <Setter Property="Foreground" Value="White" />
                                            <Setter Property="BorderBrush" Value="{DynamicResource AppAccent}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button
                            Command="{Binding SetDailyPnlPeriodCommand}"
                            CommandParameter="90"
                            Content="90 天">
                            <Button.Style>
                                <Style BasedOn="{StaticResource AssetTypeFilterChipBase}" TargetType="Button">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsPeriod90}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource AppAccent}" />
                                            <Setter Property="Foreground" Value="White" />
                                            <Setter Property="BorderBrush" Value="{DynamicResource AppAccent}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                    <lvc:CartesianChart
                        Grid.Row="2"
                        Height="140"
                        Background="Transparent"
                        Series="{Binding DailyPnlSeries}"
                        TooltipPosition="Top"
                        TooltipTextSize="10"
                        XAxes="{Binding DailyPnlXAxes}"
                        YAxes="{Binding DailyPnlYAxes}" />
                </Grid>
            </Grid>
        </Border>

        <!--  ═══ Row 5: Quote setup notice ═══  -->
        <Border
            Grid.Row="5"
            Margin="12,10,12,0"
            Padding="12,10"
            Background="{DynamicResource AppSurface}"
            BorderBrush="{DynamicResource AppBorder}"
            BorderThickness="1"
            CornerRadius="{StaticResource Radius.Sm}"
            Visibility="{Binding ShowQuoteProviderNotice, Converter={StaticResource BooleanToVisibilityConverter}}">
            <DockPanel LastChildFill="True">
                <ds:AppIcon
                    Margin="0,0,10,0"
                    VerticalAlignment="Top"
                    FontSize="16"
                    Foreground="{DynamicResource AppAccent}"
                    Symbol="Info24" />
                <StackPanel>
                    <TextBlock
                        FontSize="{StaticResource Font.Size.Sm}"
                        FontWeight="SemiBold"
                        Foreground="{DynamicResource AppTextPrimary}"
                        Text="{DynamicResource Portfolio.QuoteSetup.Title}" />
                    <TextBlock
                        Margin="0,2,0,0"
                        FontSize="{StaticResource Font.Size.Xs}"
                        Foreground="{DynamicResource AppTextSecondary}"
                        Text="{DynamicResource Portfolio.QuoteSetup.Message}"
                        TextWrapping="Wrap" />
                </StackPanel>
            </DockPanel>
        </Border>

        <!--  ═══ Row 6: Text search ═══  -->
        <controls:SearchBox
            Grid.Row="6"
            Margin="0,12,0,0"
            Placeholder="{DynamicResource Portfolio.Filter.SearchPositions}"
            Text="{Binding FilterText}">
            <controls:SearchBox.RightContent>
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <CheckBox
                        Margin="0,0,8,0"
                        VerticalAlignment="Center"
                        IsChecked="{Binding ShowClosedPositions}">
                        <TextBlock
                            FontSize="{StaticResource Font.Size.Xs}"
                            Foreground="{DynamicResource AppTextSecondary}"
                            Text="{DynamicResource Portfolio.Position.ShowClosed}" />
                    </CheckBox>
                    <!--  「加入觀察」入口已併入頂部「＋新增交易」的 ▾ 下拉，移除此處獨立按鈕避免兩顆相似入口混淆  -->
                </StackPanel>
            </controls:SearchBox.RightContent>
        </controls:SearchBox>

        <!--  ═══ Row 7: Positions DataGrid ═══  -->
        <Grid Grid.Row="7">
            <!--  Empty state  -->
            <Border
                Padding="32"
                Background="{DynamicResource AppBackground}"
                Visibility="{Binding HasNoPositions, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock
                        Margin="0,0,0,12"
                        HorizontalAlignment="Center"
                        FontSize="{StaticResource Font.Size.3xl}"
                        Opacity="0.45"
                        Text="&#x1F4BC;" />
                    <TextBlock
                        HorizontalAlignment="Center"
                        FontSize="{StaticResource Font.Size.Md}"
                        FontWeight="SemiBold"
                        Foreground="{DynamicResource AppTextPrimary}"
                        Text="{DynamicResource Portfolio.Empty}" />
                    <TextBlock
                        Margin="0,6,0,20"
                        HorizontalAlignment="Center"
                        FontSize="{StaticResource Font.Size.Sm}"
                        Foreground="{DynamicResource AppTextSecondary}"
                        Text="{DynamicResource Portfolio.Empty.Hint}" />
                    <Button
                        Height="36"
                        Padding="20,0"
                        HorizontalAlignment="Center"
                        Command="{Binding AddRecordCommand}"
                        Style="{StaticResource AppPrimaryButton}">
                        <TextBlock Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{DynamicResource Portfolio.Empty.Cta}" />
                    </Button>
                </StackPanel>
            </Border>

            <ListBox
                Margin="0,8,0,12"
                HorizontalContentAlignment="Stretch"
                Background="Transparent"
                BorderThickness="0"
                ItemsSource="{Binding PositionsView}"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                SelectedItem="{Binding SelectedPositionRow, Mode=TwoWay}">
                <!--  GroupStyle removed: PositionGroupHeaderTemplate deleted (Task 1.6 — ViewMode machinery gone)  -->
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="HorizontalAlignment" Value="Stretch" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.Style>
                    <Style TargetType="ListBox">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Value="True">
                                <DataTrigger.Binding>
                                    <MultiBinding Converter="{StaticResource WidthScaleThresholdToBooleanConverter}" ConverterParameter="1180">
                                        <Binding ElementName="Root" Path="ActualWidth" />
                                        <Binding Path="DataContext.Settings.UiScale" RelativeSource="{RelativeSource AncestorType=Window}" />
                                    </MultiBinding>
                                </DataTrigger.Binding>
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.Style>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border
                            Margin="0,0,0,10"
                            Padding="14,12"
                            CornerRadius="{StaticResource Radius.Md}">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="{DynamicResource AppSurface}" />
                                    <Setter Property="BorderBrush" Value="{DynamicResource AppBorder}" />
                                    <Setter Property="BorderThickness" Value="1" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource AppHover}" />
                                            <Setter Property="BorderBrush" Value="{DynamicResource AppBorderStrong}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource AppSelection}" />
                                            <Setter Property="BorderBrush" Value="{DynamicResource AppAccent}" />
                                            <Setter Property="BorderThickness" Value="1.5" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <DockPanel>
                                    <Grid
                                        Width="30"
                                        Height="30"
                                        Margin="0,0,10,0"
                                        VerticalAlignment="Center"
                                        DockPanel.Dock="Left">
                                        <Ellipse Opacity="0.18">
                                            <Ellipse.Fill>
                                                <SolidColorBrush Color="{Binding AssetAccentColor}" />
                                            </Ellipse.Fill>
                                        </Ellipse>
                                        <TextBlock
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            FontFamily="{StaticResource Font.Family.Base}"
                                            FontSize="{StaticResource Font.Size.Sm}"
                                            FontWeight="SemiBold"
                                            Text="{Binding AssetInitial}">
                                            <TextBlock.Foreground>
                                                <SolidColorBrush Color="{Binding AssetAccentColor}" />
                                            </TextBlock.Foreground>
                                        </TextBlock>
                                    </Grid>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="72" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock
                                        Grid.Row="0"
                                        Grid.Column="0"
                                        Margin="0,0,10,0"
                                        VerticalAlignment="Center"
                                        FontFamily="{StaticResource Font.Family.Base}"
                                        FontSize="{StaticResource Font.Size.Sm}"
                                        FontWeight="SemiBold"
                                        Foreground="{DynamicResource AppTextPrimary}"
                                        Text="{Binding Symbol}"
                                        Typography.NumeralAlignment="Tabular" />

                                    <Border
                                        Grid.Row="0"
                                        Grid.Column="1"
                                        Margin="0,0,10,0"
                                        Style="{StaticResource PortfolioAssetTypeBadge}">
                                        <TextBlock Style="{StaticResource PortfolioBadgeText}" Text="{Binding AssetTypeBadgeLabel}" />
                                    </Border>

                                    <!--
                                        P5.11 — card view 也拿掉 chip 背景，只剩純文字。
                                        用 AppTextSecondary 弱化讓視覺主軸落在 Symbol + Name 而非幣別代碼。
                                    -->
                                    <TextBlock
                                        Grid.Row="0"
                                        Grid.Column="2"
                                        Margin="0,0,10,0"
                                        VerticalAlignment="Center"
                                        FontFamily="{StaticResource Font.Family.Base}"
                                        FontSize="{StaticResource Font.Size.Xs}"
                                        Foreground="{DynamicResource AppTextSecondary}"
                                        Text="{Binding CurrencyDisplay}"
                                        Typography.NumeralAlignment="Tabular" />

                                    <TextBlock
                                        Grid.Row="1"
                                        Grid.Column="0"
                                        Grid.ColumnSpan="3"
                                        Margin="0,6,0,0"
                                        FontSize="{StaticResource Font.Size.Sm}"
                                        Foreground="{DynamicResource AppTextSecondary}"
                                        Text="{Binding Name}"
                                        TextTrimming="CharacterEllipsis" />
                                </Grid>
                                </DockPanel>

                                <Border
                                    Margin="0,10,0,0"
                                    BorderBrush="{DynamicResource AppBorder}"
                                    BorderThickness="0,1,0,0"
                                    Opacity="0.7" />

                                <UniformGrid
                                    Margin="0,10,0,0"
                                    Columns="3"
                                    Rows="3">
                                    <StackPanel Margin="0,0,12,4">
                                        <TextBlock
                                            FontSize="{StaticResource Font.Size.Xs}"
                                            Foreground="{DynamicResource AppTextMuted}"
                                            Text="{DynamicResource Portfolio.Shares}" />
                                        <TextBlock
                                            FontFamily="{StaticResource Font.Family.Base}"
                                            FontSize="{StaticResource Font.Size.Sm}"
                                            Foreground="{DynamicResource AppTextPrimary}"
                                            Text="{Binding QuantityDisplay}"
                                            Typography.NumeralAlignment="Tabular" />
                                    </StackPanel>
                                    <StackPanel Margin="0,0,12,4">
                                        <TextBlock
                                            FontSize="{StaticResource Font.Size.Xs}"
                                            Foreground="{DynamicResource AppTextMuted}"
                                            Text="{DynamicResource Portfolio.CostPerShare}" />
                                        <TextBlock
                                            FontFamily="{StaticResource Font.Family.Base}"
                                            FontSize="{StaticResource Font.Size.Sm}"
                                            Foreground="{DynamicResource AppTextSecondary}"
                                            Text="{Binding BuyPriceAsMoney, Converter={StaticResource CurrencyConverter}, ConverterParameter=price}"
                                            Typography.NumeralAlignment="Tabular" />
                                    </StackPanel>
                                    <StackPanel Margin="0,0,0,4">
                                        <TextBlock
                                            FontSize="{StaticResource Font.Size.Xs}"
                                            Foreground="{DynamicResource AppTextMuted}"
                                            Text="{DynamicResource Portfolio.TotalCostPaid}" />
                                        <TextBlock
                                            FontFamily="{StaticResource Font.Family.Base}"
                                            FontSize="{StaticResource Font.Size.Sm}"
                                            Foreground="{DynamicResource AppTextPrimary}"
                                            Text="{Binding CostAsMoney, Converter={StaticResource CurrencyConverter}, ConverterParameter=amount}"
                                            Typography.NumeralAlignment="Tabular" />
                                    </StackPanel>
                                    <StackPanel Margin="0,8,12,0">
                                        <TextBlock
                                            FontSize="{StaticResource Font.Size.Xs}"
                                            Foreground="{DynamicResource AppTextMuted}"
                                            Text="{DynamicResource Portfolio.Price}" />
                                        <TextBlock
                                            FontFamily="{StaticResource Font.Family.Base}"
                                            FontSize="{StaticResource Font.Size.Sm}"
                                            Foreground="{DynamicResource AppTextPrimary}"
                                            Text="{Binding CurrentPriceAsMoney, Converter={StaticResource CurrencyConverter}, ConverterParameter=price}"
                                            Typography.NumeralAlignment="Tabular" />
                                        <Border
                                            Margin="0,3,0,0"
                                            Style="{StaticResource StatusBadge.Info}"
                                            ToolTip="{Binding QuoteProviderStateMessage}"
                                            Visibility="{Binding ShowQuoteProviderState, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <TextBlock Style="{StaticResource StatusBadge.Text}" Text="{DynamicResource Portfolio.QuoteState.Stale}" />
                                        </Border>
                                    </StackPanel>
                                    <StackPanel Margin="0,8,12,0">
                                        <TextBlock
                                            FontSize="{StaticResource Font.Size.Xs}"
                                            Foreground="{DynamicResource AppTextMuted}"
                                            Text="{DynamicResource Common.MarketValue}" />
                                        <TextBlock
                                            FontFamily="{StaticResource Font.Family.Base}"
                                            FontSize="{StaticResource Font.Size.Sm}"
                                            Foreground="{DynamicResource AppTextPrimary}"
                                            Text="{Binding MarketValueAsMoney, Converter={StaticResource CurrencyConverter}, ConverterParameter=amount}"
                                            Typography.NumeralAlignment="Tabular" />
                                    </StackPanel>
                                    <StackPanel Margin="0,8,0,0">
                                        <TextBlock
                                            FontSize="{StaticResource Font.Size.Xs}"
                                            Foreground="{DynamicResource AppTextMuted}"
                                            Text="{DynamicResource Portfolio.NetVal}" />
                                        <TextBlock
                                            FontFamily="{StaticResource Font.Family.Base}"
                                            FontSize="{StaticResource Font.Size.Sm}"
                                            Foreground="{DynamicResource AppTextPrimary}"
                                            Text="{Binding NetValueAsMoney, Converter={StaticResource CurrencyConverter}, ConverterParameter=amount}"
                                            Typography.NumeralAlignment="Tabular" />
                                    </StackPanel>
                                    <StackPanel Margin="0,8,12,0">
                                        <TextBlock
                                            FontSize="{StaticResource Font.Size.Xs}"
                                            Foreground="{DynamicResource AppTextMuted}"
                                            Text="{DynamicResource Portfolio.PnL}" />
                                        <TextBlock
                                            FontFamily="{StaticResource Font.Family.Base}"
                                            FontSize="{StaticResource Font.Size.Sm}"
                                            FontWeight="SemiBold"
                                            Text="{Binding PnlAsMoney, Converter={StaticResource CurrencyConverter}, ConverterParameter=signed}"
                                            Typography.NumeralAlignment="Tabular">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{DynamicResource AppDown}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsPnlPositive}" Value="True">
                                                            <Setter Property="Foreground" Value="{DynamicResource AppUp}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Pnl}" Value="0">
                                                            <Setter Property="Foreground" Value="{DynamicResource AppTextMuted}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                    <StackPanel Margin="0,8,0,0">
                                        <TextBlock
                                            FontSize="{StaticResource Font.Size.Xs}"
                                            Foreground="{DynamicResource AppTextMuted}"
                                            Text="{DynamicResource Portfolio.PercentOfPortfolio}" />
                                        <TextBlock
                                            FontFamily="{StaticResource Font.Family.Base}"
                                            FontSize="{StaticResource Font.Size.Sm}"
                                            Foreground="{DynamicResource AppTextSecondary}"
                                            Text="{Binding PercentOfPortfolio, StringFormat={}{0:F1}%}"
                                            Typography.NumeralAlignment="Tabular" />
                                    </StackPanel>
                                </UniformGrid>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <DataGrid
                x:Name="PortfolioGrid"
                AlternationCount="2"
                AutoGenerateColumns="False"
                Background="{DynamicResource AppBackground}"
                BorderThickness="0"
                CanUserAddRows="False"
                CanUserDeleteRows="False"
                CanUserReorderColumns="False"
                CanUserResizeColumns="False"
                CanUserSortColumns="True"
                ColumnHeaderHeight="{StaticResource DataGrid.HeaderHeight}"
                EnableRowVirtualization="True"
                FontFamily="{StaticResource Font.Family.Base}"
                FontSize="{StaticResource Font.Size.Sm}"
                Foreground="{DynamicResource AppTextPrimary}"
                GridLinesVisibility="None"
                HeadersVisibility="Column"
                HorizontalScrollBarVisibility="Auto"
                IsReadOnly="True"
                ItemsSource="{Binding PositionsView}"
                PreviewMouseDown="OnGridPreviewMouseDown"
                RowHeight="{StaticResource DataGrid.RowHeight}"
                SelectedItem="{Binding SelectedPositionRow, Mode=TwoWay}"
                SelectionMode="Single"
                VirtualizingPanel.IsVirtualizing="True"
                VirtualizingPanel.VirtualizationMode="Recycling">
                <DataGrid.Style>
                    <Style TargetType="DataGrid">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Value="True">
                                <DataTrigger.Binding>
                                    <MultiBinding Converter="{StaticResource WidthScaleThresholdToBooleanConverter}" ConverterParameter="1180">
                                        <Binding ElementName="Root" Path="ActualWidth" />
                                        <Binding Path="DataContext.Settings.UiScale" RelativeSource="{RelativeSource AncestorType=Window}" />
                                    </MultiBinding>
                                </DataTrigger.Binding>
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Style>

                <DataGrid.ColumnHeaderStyle>
                    <Style BasedOn="{StaticResource AppDataGridColHeader}" TargetType="DataGridColumnHeader" />
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.CellStyle>
                    <Style BasedOn="{StaticResource AppDataGridCell}" TargetType="DataGridCell" />
                </DataGrid.CellStyle>
                <DataGrid.RowStyle>
                    <Style BasedOn="{StaticResource AppDataGridRowSelectable}" TargetType="DataGridRow" />
                </DataGrid.RowStyle>

                <DataGrid.Columns>
                    <!--  資產 — 代號 + 類型 badge + 名稱 + 幣別（v3：幣別折回此欄，僅外幣顯示；TWD 為 base 隱含）  -->
                    <DataGridTemplateColumn
                        Width="*"
                        MinWidth="200"
                        SortMemberPath="Symbol">
                        <DataGridTemplateColumn.Header>
                            <TextBlock Text="{DynamicResource Portfolio.AssetWithCurrency}" />
                        </DataGridTemplateColumn.Header>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid VerticalAlignment="Center">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40" />
                                        <ColumnDefinition Width="72" />
                                        <ColumnDefinition Width="42" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <!--  資產 logo 圓點：色點＋首字（色由 Symbol hash 決定，深淺主題自動套色）  -->
                                    <Grid
                                        Grid.Column="0"
                                        Width="28"
                                        Height="28"
                                        Margin="2,0,10,0"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center">
                                        <Ellipse Opacity="0.18">
                                            <Ellipse.Fill>
                                                <SolidColorBrush Color="{Binding AssetAccentColor}" />
                                            </Ellipse.Fill>
                                        </Ellipse>
                                        <TextBlock
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            FontFamily="{StaticResource Font.Family.Base}"
                                            FontSize="{StaticResource Font.Size.Xs}"
                                            FontWeight="SemiBold"
                                            Text="{Binding AssetInitial}">
                                            <TextBlock.Foreground>
                                                <SolidColorBrush Color="{Binding AssetAccentColor}" />
                                            </TextBlock.Foreground>
                                        </TextBlock>
                                    </Grid>
                                    <TextBlock
                                        Grid.Column="1"
                                        Margin="0,0,10,0"
                                        VerticalAlignment="Center"
                                        FontFamily="{StaticResource Font.Family.Base}"
                                        FontSize="{StaticResource Font.Size.Sm}"
                                        FontWeight="SemiBold"
                                        Foreground="{DynamicResource AppTextPrimary}"
                                        Text="{Binding Symbol}"
                                        Typography.NumeralAlignment="Tabular" />
                                    <Border
                                        Grid.Column="2"
                                        Margin="0,0,10,0"
                                        HorizontalAlignment="Center"
                                        Style="{StaticResource PortfolioAssetTypeBadge}">
                                        <TextBlock Style="{StaticResource PortfolioBadgeText}" Text="{Binding AssetTypeBadgeLabel}" />
                                    </Border>
                                    <TextBlock
                                        Grid.Column="3"
                                        VerticalAlignment="Center"
                                        FontFamily="{StaticResource Font.Family.Base}"
                                        FontSize="{StaticResource Font.Size.Sm}"
                                        Foreground="{DynamicResource AppTextPrimary}"
                                        Text="{Binding Name}"
                                        TextTrimming="CharacterEllipsis" />
                                    <!--  幣別：折回資產欄，僅外幣（cross-currency）顯示；base(TWD) 隱含不顯示  -->
                                    <TextBlock
                                        Grid.Column="4"
                                        Margin="8,0,0,0"
                                        VerticalAlignment="Center"
                                        FontFamily="{StaticResource Font.Family.Base}"
                                        FontSize="{StaticResource Font.Size.Xs}"
                                        Foreground="{DynamicResource AppTextMuted}"
                                        Text="{Binding CurrencyDisplay}"
                                        Typography.NumeralAlignment="Tabular"
                                        Visibility="{Binding IsCrossCurrency, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!--  股數  -->
                    <DataGridTemplateColumn Width="150" SortMemberPath="Quantity">
                        <DataGridTemplateColumn.HeaderStyle>
                            <Style BasedOn="{StaticResource AppDataGridColHeader}" TargetType="DataGridColumnHeader">
                                <Setter Property="HorizontalContentAlignment" Value="Right" />
                            </Style>
                        </DataGridTemplateColumn.HeaderStyle>
                        <DataGridTemplateColumn.Header>
                            <TextBlock Text="{DynamicResource Portfolio.Shares}" />
                        </DataGridTemplateColumn.Header>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <!--  v4 視覺層級：數量為「參考」欄 → 轉淡（AppTextMuted），讓現價/損益/市值的決策數字突出。  -->
                                <TextBlock
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    FontFamily="{StaticResource Font.Family.Base}"
                                    FontSize="{StaticResource Font.Size.Sm}"
                                    Foreground="{DynamicResource AppTextMuted}"
                                    Text="{Binding QuantityDisplay}"
                                    Typography.NumeralAlignment="Tabular" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!--  成本/股（含手續費）  -->
                    <DataGridTemplateColumn Width="120" SortMemberPath="BuyPrice">
                        <DataGridTemplateColumn.HeaderStyle>
                            <Style BasedOn="{StaticResource AppDataGridColHeader}" TargetType="DataGridColumnHeader">
                                <Setter Property="HorizontalContentAlignment" Value="Right" />
                            </Style>
                        </DataGridTemplateColumn.HeaderStyle>
                        <DataGridTemplateColumn.Header>
                            <TextBlock Text="{DynamicResource Portfolio.CostPerShare.Native}" />
                        </DataGridTemplateColumn.Header>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <!--  v4 視覺層級：成本均價為「參考」欄 → 轉淡。  -->
                                    <TextBlock
                                        HorizontalAlignment="Right"
                                        FontFamily="{StaticResource Font.Family.Base}"
                                        FontSize="{StaticResource Font.Size.Sm}"
                                        Foreground="{DynamicResource AppTextMuted}"
                                        Text="{Binding BuyPriceAsMoney, Converter={StaticResource CurrencyConverter}, ConverterParameter=price}"
                                        Typography.NumeralAlignment="Tabular" />
                                    <TextBlock
                                        HorizontalAlignment="Right"
                                        FontFamily="{StaticResource Font.Family.Base}"
                                        FontSize="{StaticResource Font.Size.Xs}"
                                        Foreground="{DynamicResource AppTextMuted}"
                                        Text="{Binding BuyPriceBaseAsMoney, Converter={StaticResource CurrencyConverter}, ConverterParameter=price-approx}"
                                        Typography.NumeralAlignment="Tabular"
                                        Visibility="{Binding IsCrossCurrency, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!--  付出成本（含買入手續費）  -->
                    <DataGridTemplateColumn Width="150" SortMemberPath="Cost">
                        <DataGridTemplateColumn.HeaderStyle>
                            <Style BasedOn="{StaticResource AppDataGridColHeader}" TargetType="DataGridColumnHeader">
                                <Setter Property="HorizontalContentAlignment" Value="Right" />
                            </Style>
                        </DataGridTemplateColumn.HeaderStyle>
                        <DataGridTemplateColumn.Header>
                            <TextBlock Text="{DynamicResource Portfolio.TotalCostPaid.Native}" />
                        </DataGridTemplateColumn.Header>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <!--  v4 視覺層級：付出成本為「參考」欄 → 轉淡。  -->
                                    <TextBlock
                                        HorizontalAlignment="Right"
                                        FontFamily="{StaticResource Font.Family.Base}"
                                        FontSize="{StaticResource Font.Size.Sm}"
                                        Foreground="{DynamicResource AppTextMuted}"
                                        Text="{Binding CostAsMoney, Converter={StaticResource CurrencyConverter}, ConverterParameter=amount}"
                                        Typography.NumeralAlignment="Tabular" />
                                    <TextBlock
                                        HorizontalAlignment="Right"
                                        FontFamily="{StaticResource Font.Family.Base}"
                                        FontSize="{StaticResource Font.Size.Xs}"
                                        Foreground="{DynamicResource AppTextMuted}"
                                        Text="{Binding CostBaseAsMoney, Converter={StaticResource CurrencyConverter}, ConverterParameter=amount-approx}"
                                        Typography.NumeralAlignment="Tabular"
                                        Visibility="{Binding IsCrossCurrency, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!--  現價（合併今日漲跌% 小字）  -->
                    <DataGridTemplateColumn Width="120" SortMemberPath="CurrentPrice">
                        <DataGridTemplateColumn.HeaderStyle>
                            <Style BasedOn="{StaticResource AppDataGridColHeader}" TargetType="DataGridColumnHeader">
                                <Setter Property="HorizontalContentAlignment" Value="Right" />
                            </Style>
                        </DataGridTemplateColumn.HeaderStyle>
                        <DataGridTemplateColumn.Header>
                            <TextBlock Text="{DynamicResource Portfolio.Price.Native}" />
                        </DataGridTemplateColumn.Header>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid>
                                    <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center">
                                        <TextBlock
                                            HorizontalAlignment="Right"
                                            FontFamily="{StaticResource Font.Family.Base}"
                                            FontSize="{StaticResource Font.Size.Sm}"
                                            FontWeight="SemiBold"
                                            Foreground="{DynamicResource AppTextPrimary}"
                                            Text="{Binding CurrentPriceAsMoney, Converter={StaticResource CurrencyConverter}, ConverterParameter=price}"
                                            Typography.NumeralAlignment="Tabular">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding CurrentPrice}" Value="0">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock
                                            HorizontalAlignment="Right"
                                            FontFamily="{StaticResource Font.Family.Base}"
                                            FontSize="{StaticResource Font.Size.Xs}"
                                            Foreground="{DynamicResource AppTextMuted}"
                                            Text="{Binding CurrentPriceBaseAsMoney, Converter={StaticResource CurrencyConverter}, ConverterParameter=price-approx}"
                                            Typography.NumeralAlignment="Tabular"
                                            Visibility="{Binding IsCrossCurrency, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                        <!--  今日 %（色彩跟漲跌連動）  -->
                                        <TextBlock
                                            HorizontalAlignment="Right"
                                            FontFamily="{StaticResource Font.Family.Base}"
                                            FontSize="{StaticResource Font.Size.Xs}"
                                            Text="{Binding DayChangePercent, StringFormat={}{0:+0.00;-0.00;0.00}%}"
                                            Typography.NumeralAlignment="Tabular">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{DynamicResource AppDown}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsDayChangePositive}" Value="True">
                                                            <Setter Property="Foreground" Value="{DynamicResource AppUp}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding DayChange}" Value="0">
                                                            <Setter Property="Foreground" Value="{DynamicResource AppTextMuted}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding PrevClose}" Value="0">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <Border
                                            Margin="0,3,0,0"
                                            HorizontalAlignment="Right"
                                            Style="{StaticResource StatusBadge.Info}"
                                            ToolTip="{Binding QuoteProviderStateMessage}"
                                            Visibility="{Binding ShowQuoteProviderState, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <TextBlock Style="{StaticResource StatusBadge.Text}" Text="{DynamicResource Portfolio.QuoteState.Stale}" />
                                        </Border>
                                    </StackPanel>
                                    <TextBlock
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        FontFamily="{StaticResource Font.Family.Base}"
                                        FontSize="{StaticResource Font.Size.Sm}"
                                        Foreground="{DynamicResource AppTextMuted}"
                                        Text="…"
                                        Typography.NumeralAlignment="Tabular"
                                        Visibility="{Binding IsLoadingPrice, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!--  淨損益試算（淨值 - 付出成本）  -->
                    <DataGridTemplateColumn Width="180" SortMemberPath="Pnl">
                        <DataGridTemplateColumn.HeaderStyle>
                            <Style BasedOn="{StaticResource AppDataGridColHeader}" TargetType="DataGridColumnHeader">
                                <Setter Property="HorizontalContentAlignment" Value="Right" />
                            </Style>
                        </DataGridTemplateColumn.HeaderStyle>
                        <DataGridTemplateColumn.Header>
                            <TextBlock
                                Text="{DynamicResource Portfolio.PnL.Native}"
                                ToolTip="{DynamicResource Portfolio.PnL.NetTooltip}"
                                ToolTipService.InitialShowDelay="300"
                                ToolTipService.ShowDuration="20000" />
                        </DataGridTemplateColumn.Header>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid ToolTip="{DynamicResource Portfolio.PnL.NetTooltip}" ToolTipService.InitialShowDelay="500">
                                    <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center">
                                        <!--  v4 視覺層級：淨損益＝整列主角 → 字級放大（Md）、加粗、依漲跌上色。  -->
                                        <TextBlock
                                            HorizontalAlignment="Right"
                                            FontFamily="{StaticResource Font.Family.Base}"
                                            FontSize="{StaticResource Font.Size.Md}"
                                            FontWeight="SemiBold"
                                            Text="{Binding PnlAsMoney, Converter={StaticResource CurrencyConverter}, ConverterParameter=signed}"
                                            Typography.NumeralAlignment="Tabular">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{DynamicResource AppDown}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsPnlPositive}" Value="True">
                                                            <Setter Property="Foreground" Value="{DynamicResource AppUp}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Pnl}" Value="0">
                                                            <Setter Property="Foreground" Value="{DynamicResource AppTextMuted}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock
                                            HorizontalAlignment="Right"
                                            FontFamily="{StaticResource Font.Family.Base}"
                                            FontSize="{StaticResource Font.Size.Xs}"
                                            Text="{Binding PnlPercent, StringFormat={}{0:+0.00;-0.00;0.00}%}"
                                            Typography.NumeralAlignment="Tabular">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{DynamicResource AppDown}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsPnlPositive}" Value="True">
                                                            <Setter Property="Foreground" Value="{DynamicResource AppUp}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Pnl}" Value="0">
                                                            <Setter Property="Foreground" Value="{DynamicResource AppTextMuted}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock
                                            HorizontalAlignment="Right"
                                            FontFamily="{StaticResource Font.Family.Base}"
                                            FontSize="{StaticResource Font.Size.Xs}"
                                            Foreground="{DynamicResource AppTextMuted}"
                                            Text="{Binding PnlBaseAsMoney, Converter={StaticResource CurrencyConverter}, ConverterParameter=signed-approx}"
                                            Typography.NumeralAlignment="Tabular"
                                            Visibility="{Binding IsCrossCurrency, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                    </StackPanel>
                                    <TextBlock
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        FontFamily="{StaticResource Font.Family.Base}"
                                        FontSize="{StaticResource Font.Size.Sm}"
                                        Foreground="{DynamicResource AppTextMuted}"
                                        Text="…"
                                        Typography.NumeralAlignment="Tabular"
                                        Visibility="{Binding IsLoadingPrice, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!--
                        市值（未扣預估賣出費/稅）— 最右側欄位。
                        淨值（市值扣預估賣出費/稅）折進此欄第二行（小字＋tooltip）；幣別折進資產欄（僅外幣）。
                        資產表 7 欄：資產(含幣別) / 數量 / 成本均價 / 付出成本 / 現價 / 淨損益試算 / 市值(含淨值)。
                        ——「詳細但去冗餘」：桌面寬視窗保留逐欄排序更實用，故撤銷步驟3c 的「部位」合併，
                        只折真冗餘的 幣別(多為 TWD) 與 淨值(≈市值)。
                    -->
                    <DataGridTemplateColumn Width="150" SortMemberPath="MarketValue">
                        <DataGridTemplateColumn.HeaderStyle>
                            <Style BasedOn="{StaticResource AppDataGridColHeader}" TargetType="DataGridColumnHeader">
                                <Setter Property="HorizontalContentAlignment" Value="Right" />
                            </Style>
                        </DataGridTemplateColumn.HeaderStyle>
                        <DataGridTemplateColumn.Header>
                            <TextBlock Text="{DynamicResource Portfolio.MarketValue.Native}" />
                        </DataGridTemplateColumn.Header>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid>
                                    <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center">
                                        <TextBlock
                                            HorizontalAlignment="Right"
                                            FontFamily="{StaticResource Font.Family.Base}"
                                            FontSize="{StaticResource Font.Size.Sm}"
                                            FontWeight="SemiBold"
                                            Foreground="{DynamicResource AppTextPrimary}"
                                            Text="{Binding MarketValueAsMoney, Converter={StaticResource CurrencyConverter}, ConverterParameter=amount}"
                                            Typography.NumeralAlignment="Tabular" />
                                        <TextBlock
                                            HorizontalAlignment="Right"
                                            FontFamily="{StaticResource Font.Family.Base}"
                                            FontSize="{StaticResource Font.Size.Xs}"
                                            Foreground="{DynamicResource AppTextMuted}"
                                            Text="{Binding MarketValueBaseAsMoney, Converter={StaticResource CurrencyConverter}, ConverterParameter=amount-approx}"
                                            Typography.NumeralAlignment="Tabular"
                                            Visibility="{Binding IsCrossCurrency, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                        <!--  淨值（市值扣預估賣出費/稅）— v3 由獨立欄折進市值第二行  -->
                                        <StackPanel
                                            Margin="0,2,0,0"
                                            HorizontalAlignment="Right"
                                            Orientation="Horizontal"
                                            ToolTip="{DynamicResource Portfolio.NetVal.Tooltip}"
                                            ToolTipService.InitialShowDelay="500">
                                            <TextBlock
                                                Margin="0,0,4,0"
                                                FontFamily="{StaticResource Font.Family.Base}"
                                                FontSize="{StaticResource Font.Size.Xs}"
                                                Foreground="{DynamicResource AppTextMuted}"
                                                Text="{DynamicResource Portfolio.NetVal.Native}" />
                                            <TextBlock
                                                FontFamily="{StaticResource Font.Family.Base}"
                                                FontSize="{StaticResource Font.Size.Xs}"
                                                Foreground="{DynamicResource AppTextMuted}"
                                                Text="{Binding NetValueAsMoney, Converter={StaticResource CurrencyConverter}, ConverterParameter=amount}"
                                                Typography.NumeralAlignment="Tabular" />
                                        </StackPanel>
                                    </StackPanel>
                                    <TextBlock
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        FontFamily="{StaticResource Font.Family.Base}"
                                        FontSize="{StaticResource Font.Size.Sm}"
                                        Foreground="{DynamicResource AppTextMuted}"
                                        Text="…"
                                        Typography.NumeralAlignment="Tabular"
                                        Visibility="{Binding IsLoadingPrice, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!--  Empty state  -->
            <Border
                Padding="32"
                Background="{DynamicResource AppBackground}"
                Visibility="{Binding HasNoPositions, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock
                        Margin="0,0,0,12"
                        HorizontalAlignment="Center"
                        FontSize="{StaticResource Font.Size.3xl}"
                        Opacity="0.45"
                        Text="&#x1F4BC;" />
                    <TextBlock
                        HorizontalAlignment="Center"
                        FontSize="{StaticResource Font.Size.Md}"
                        FontWeight="SemiBold"
                        Foreground="{DynamicResource AppTextPrimary}"
                        Text="{DynamicResource Portfolio.Empty}" />
                    <TextBlock
                        Margin="0,6,0,20"
                        HorizontalAlignment="Center"
                        FontSize="{StaticResource Font.Size.Sm}"
                        Foreground="{DynamicResource AppTextSecondary}"
                        Text="{DynamicResource Portfolio.Empty.Hint}" />
                    <Button
                        Height="36"
                        Padding="20,0"
                        HorizontalAlignment="Center"
                        Command="{Binding AddRecordCommand}"
                        Style="{StaticResource AppPrimaryButton}">
                        <TextBlock Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{DynamicResource Portfolio.Empty.Cta}" />
                    </Button>
                </StackPanel>
            </Border>
        </Grid>

        <!--
            ═══ Row 8: Filter Summary Footer ═══
            篩選作用中才顯示（IsPositionsFiltered=true）。仿 TradesTabPanel 的「流入 /
            流出 / 淨額」footer pattern，讓使用者切資產類型 chip 後立即看到該類別
            的彙總（筆數 / 市值 / 成本 / 損益），不必心算。
        -->
        <!--
            Footer 永遠顯示 — 「全部」也是篩選的一種（無條件全留），看見整體合計
            跟切資產類型後的子集合計一樣有用，不該只在 chip 不是全部時才顯示。
        -->
        <Border
            Grid.Row="8"
            Padding="12,6"
            Background="{DynamicResource AppSurface}"
            BorderBrush="{DynamicResource AppBorder}"
            BorderThickness="0,1,0,0">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                <StackPanel Orientation="Horizontal">
                    <StackPanel Margin="0,0,24,0" Orientation="Horizontal">
                        <TextBlock
                            Margin="0,0,6,0"
                            VerticalAlignment="Center"
                            FontSize="{StaticResource Font.Size.Xs}"
                            Foreground="{DynamicResource AppTextMuted}"
                            Text="{DynamicResource Portfolio.Filter.Summary.Count}" />
                        <TextBlock
                            VerticalAlignment="Center"
                            FontFamily="{StaticResource Font.Family.Base}"
                            FontSize="{StaticResource Font.Size.Sm}"
                            FontWeight="SemiBold"
                            Foreground="{DynamicResource AppTextPrimary}"
                            Text="{Binding PositionsFilteredCount, StringFormat={}{0:N0}}"
                            Typography.NumeralAlignment="Tabular" />
                    </StackPanel>
                    <StackPanel Margin="0,0,24,0" Orientation="Horizontal">
                        <TextBlock
                            Margin="0,0,6,0"
                            VerticalAlignment="Center"
                            FontSize="{StaticResource Font.Size.Xs}"
                            Foreground="{DynamicResource AppTextMuted}"
                            Text="{DynamicResource Portfolio.Filter.Summary.MarketValue}" />
                        <TextBlock
                            VerticalAlignment="Center"
                            FontFamily="{StaticResource Font.Family.Base}"
                            FontSize="{StaticResource Font.Size.Sm}"
                            FontWeight="SemiBold"
                            Foreground="{DynamicResource AppTextPrimary}"
                            Text="{Binding PositionsFilteredMarketValue, Converter={StaticResource CurrencyConverter}, ConverterParameter=amount}"
                            Typography.NumeralAlignment="Tabular" />
                    </StackPanel>
                    <StackPanel Margin="0,0,24,0" Orientation="Horizontal">
                        <TextBlock
                            Margin="0,0,6,0"
                            VerticalAlignment="Center"
                            FontSize="{StaticResource Font.Size.Xs}"
                            Foreground="{DynamicResource AppTextMuted}"
                            Text="{DynamicResource Portfolio.Filter.Summary.Cost}" />
                        <TextBlock
                            VerticalAlignment="Center"
                            FontFamily="{StaticResource Font.Family.Base}"
                            FontSize="{StaticResource Font.Size.Sm}"
                            Foreground="{DynamicResource AppTextSecondary}"
                            Text="{Binding PositionsFilteredCost, Converter={StaticResource CurrencyConverter}, ConverterParameter=amount}"
                            Typography.NumeralAlignment="Tabular" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock
                            Margin="0,0,6,0"
                            VerticalAlignment="Center"
                            FontSize="{StaticResource Font.Size.Xs}"
                            Foreground="{DynamicResource AppTextMuted}"
                            Text="{DynamicResource Portfolio.Filter.Summary.Pnl}" />
                        <TextBlock
                            VerticalAlignment="Center"
                            FontFamily="{StaticResource Font.Family.Base}"
                            FontSize="{StaticResource Font.Size.Sm}"
                            FontWeight="SemiBold"
                            Text="{Binding PositionsFilteredPnl, Converter={StaticResource CurrencyConverter}, ConverterParameter=signed}"
                            Typography.NumeralAlignment="Tabular">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{DynamicResource AppDown}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsPositionsFilteredPnlPositive}" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource AppUp}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding PositionsFilteredPnl}" Value="0">
                                            <Setter Property="Foreground" Value="{DynamicResource AppTextSecondary}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                    <!--  獲利／虧損 檔數 — 補回原「盈虧檔數」chip 的核心資訊  -->
                    <StackPanel Margin="24,0,0,0" Orientation="Horizontal">
                        <TextBlock
                            Margin="0,0,6,0"
                            VerticalAlignment="Center"
                            FontSize="{StaticResource Font.Size.Xs}"
                            Foreground="{DynamicResource AppTextMuted}"
                            Text="{DynamicResource Portfolio.Filter.Summary.WinLoss}" />
                        <TextBlock
                            VerticalAlignment="Center"
                            FontFamily="{StaticResource Font.Family.Base}"
                            FontSize="{StaticResource Font.Size.Sm}"
                            FontWeight="SemiBold"
                            Foreground="{DynamicResource AppUp}"
                            Text="{Binding PositionsFilteredProfitCount}"
                            Typography.NumeralAlignment="Tabular" />
                        <TextBlock
                            Margin="4,0,4,0"
                            VerticalAlignment="Center"
                            FontSize="{StaticResource Font.Size.Xs}"
                            Foreground="{DynamicResource AppTextMuted}"
                            Text="/" />
                        <TextBlock
                            VerticalAlignment="Center"
                            FontFamily="{StaticResource Font.Family.Base}"
                            FontSize="{StaticResource Font.Size.Sm}"
                            FontWeight="SemiBold"
                            Foreground="{DynamicResource AppDown}"
                            Text="{Binding PositionsFilteredLossCount}"
                            Typography.NumeralAlignment="Tabular" />
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!--
            ═══ Watchlist Add Dialog ═══
            Promoted to MainWindow.xaml so the backdrop dims the entire window
            (含 NavRail / title bar)，跟 AddRecordDialog / AddAssetDialog 一致。
        -->
    </Grid>

</UserControl>
