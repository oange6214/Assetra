<UserControl
    x:Class="Assetra.WPF.Features.Portfolio.Controls.AddAssetDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:assetSections="clr-namespace:Assetra.WPF.Features.Portfolio.Controls.AssetSections"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Assetra.WPF.Features.Portfolio"
    x:Name="Root"
    d:DataContext="{d:DesignInstance Type={x:Type vm:PortfolioViewModel},
                                     IsDesignTimeCreatable=False}"
    Visibility="{Binding AddAssetDialog.IsAddDialogOpen, Converter={StaticResource BooleanToVisibilityConverter}}"
    mc:Ignorable="d">

    <Grid>
        <!--  半透明背景幕  -->
        <Border Background="{DynamicResource AppModalOverlay}" />

        <!--  Dialog card  -->
        <Border
            MaxHeight="{StaticResource DialogMaxHeight}"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="{DynamicResource AppSurface}"
            BorderBrush="{DynamicResource AppBorder}"
            BorderThickness="1"
            CornerRadius="{StaticResource RadiusMd}">
            <Border.Width>
                <MultiBinding Converter="{StaticResource AdaptivePanelWidthConverter}" ConverterParameter="0.62|400|560">
                    <Binding ElementName="Root" Path="ActualWidth" />
                    <Binding Path="DataContext.Settings.UiScale" RelativeSource="{RelativeSource AncestorType=Window}" />
                </MultiBinding>
            </Border.Width>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <!--  Title  -->
                    <RowDefinition Height="Auto" />
                    <!--  Type selector  -->
                    <RowDefinition Height="*" />
                    <!--  Fields  -->
                    <RowDefinition Height="Auto" />
                    <!--  Error  -->
                    <RowDefinition Height="Auto" />
                    <!--  Buttons  -->
                </Grid.RowDefinitions>

                <!--  Title  -->
                <Border
                    Grid.Row="0"
                    Padding="20,14"
                    BorderBrush="{DynamicResource AppBorder}"
                    BorderThickness="0,0,0,1">
                    <DockPanel>
                        <Button
                            Width="28"
                            Height="28"
                            Margin="0,0,8,0"
                            VerticalAlignment="Center"
                            Command="{Binding AddAssetDialog.BackToTypePickerCommand}"
                            DockPanel.Dock="Left"
                            Style="{StaticResource BtnIcon}"
                            ToolTip="{DynamicResource Common.Back}"
                            Visibility="{Binding AddAssetDialog.IsLiabilityFormStep, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock
                                FontFamily="Segoe MDL2 Assets"
                                FontSize="12"
                                Foreground="{DynamicResource AppTextMuted}"
                                Text="&#xE72B;" />
                        </Button>
                        <Button
                            Width="28"
                            Height="28"
                            HorizontalAlignment="Right"
                            Command="{Binding AddAssetDialog.CloseAddDialogCommand}"
                            DockPanel.Dock="Right"
                            Style="{StaticResource BtnIcon}">
                            <TextBlock
                                FontFamily="Segoe MDL2 Assets"
                                FontSize="12"
                                Foreground="{DynamicResource AppTextMuted}"
                                Text="&#xE8BB;" />
                        </Button>
                        <TextBlock
                            VerticalAlignment="Center"
                            FontSize="{StaticResource FontSizeSm}"
                            FontWeight="SemiBold"
                            Foreground="{DynamicResource AppTextPrimary}"
                            Text="{DynamicResource Portfolio.Account.Title}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="{DynamicResource Portfolio.Account.Title}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding AddAssetDialog.IsLiabilityDialogMode}" Value="True">
                                            <Setter Property="Text" Value="{DynamicResource Portfolio.Liability.Title}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </DockPanel>
                </Border>

                <!--  Type picker (Step 1 — only shown when IsTypePickerStep)  -->
                <Grid
                    Grid.Row="1"
                    Grid.RowSpan="4"
                    Margin="20,18,20,18"
                    Visibility="{Binding AddAssetDialog.IsTypePickerStep, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock
                        Grid.Row="0"
                        Margin="0,0,0,12"
                        FontSize="{StaticResource FontSizeSm}"
                        FontWeight="SemiBold"
                        Foreground="{DynamicResource AppTextSecondary}"
                        Text="{DynamicResource Portfolio.Liability.PickType.Heading}"
                        Visibility="{Binding AddAssetDialog.IsLiabilityDialogMode, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <TextBlock
                        Grid.Row="0"
                        Margin="0,0,0,12"
                        FontSize="{StaticResource FontSizeSm}"
                        FontWeight="SemiBold"
                        Foreground="{DynamicResource AppTextSecondary}"
                        Text="{DynamicResource Portfolio.Account.PickType.Heading}"
                        Visibility="{Binding AddAssetDialog.IsAccountDialogMode, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <UniformGrid
                        Grid.Row="1"
                        Columns="3"
                        Visibility="{Binding AddAssetDialog.IsLiabilityDialogMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <!--  Card: 信用卡  -->
                        <Button
                            Margin="0,0,4,8"
                            Padding="14,16"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Background="{DynamicResource AppBackground}"
                            BorderBrush="{DynamicResource AppBorder}"
                            BorderThickness="1"
                            Command="{Binding AddAssetDialog.SelectLiabilityTypeCommand}"
                            CommandParameter="creditCard:信用卡"
                            Style="{StaticResource BtnGhost}">
                            <StackPanel HorizontalAlignment="Left">
                                <TextBlock
                                    Margin="0,0,0,6"
                                    FontSize="{StaticResource FontSize2xl}"
                                    Text="&#x1F4B3;" />
                                <TextBlock
                                    FontSize="{StaticResource FontSizeBase}"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource AppTextPrimary}"
                                    Text="{DynamicResource Portfolio.Liability.Preset.CreditCard}" />
                            </StackPanel>
                        </Button>
                        <!--  Card: 分期付款  -->
                        <Button
                            Margin="4,0,4,8"
                            Padding="14,16"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Background="{DynamicResource AppBackground}"
                            BorderBrush="{DynamicResource AppBorder}"
                            BorderThickness="1"
                            Command="{Binding AddAssetDialog.SelectLiabilityTypeCommand}"
                            CommandParameter="creditCard:0% 分期"
                            Style="{StaticResource BtnGhost}">
                            <StackPanel HorizontalAlignment="Left">
                                <TextBlock
                                    Margin="0,0,0,6"
                                    FontSize="{StaticResource FontSize2xl}"
                                    Text="&#x1F4C5;" />
                                <TextBlock
                                    FontSize="{StaticResource FontSizeBase}"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource AppTextPrimary}"
                                    Text="{DynamicResource Portfolio.Liability.Preset.Installment}" />
                            </StackPanel>
                        </Button>
                        <!--  Card: 預借現金  -->
                        <Button
                            Margin="4,0,0,8"
                            Padding="14,16"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Background="{DynamicResource AppBackground}"
                            BorderBrush="{DynamicResource AppBorder}"
                            BorderThickness="1"
                            Command="{Binding AddAssetDialog.SelectLiabilityTypeCommand}"
                            CommandParameter="creditCard:預借現金"
                            Style="{StaticResource BtnGhost}">
                            <StackPanel HorizontalAlignment="Left">
                                <TextBlock
                                    Margin="0,0,0,6"
                                    FontSize="{StaticResource FontSize2xl}"
                                    Text="&#x1F9FE;" />
                                <TextBlock
                                    FontSize="{StaticResource FontSizeBase}"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource AppTextPrimary}"
                                    Text="{DynamicResource Portfolio.Liability.Preset.CashAdvance}" />
                            </StackPanel>
                        </Button>
                        <!--  Card: 房屋貸款  -->
                        <Button
                            Margin="0,0,4,8"
                            Padding="14,16"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Background="{DynamicResource AppBackground}"
                            BorderBrush="{DynamicResource AppBorder}"
                            BorderThickness="1"
                            Command="{Binding AddAssetDialog.SelectLiabilityTypeCommand}"
                            CommandParameter="loan:房貸"
                            Style="{StaticResource BtnGhost}">
                            <StackPanel HorizontalAlignment="Left">
                                <TextBlock
                                    Margin="0,0,0,6"
                                    FontSize="{StaticResource FontSize2xl}"
                                    Text="&#x1F3E0;" />
                                <TextBlock
                                    FontSize="{StaticResource FontSizeBase}"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource AppTextPrimary}"
                                    Text="{DynamicResource Portfolio.Liability.Preset.Mortgage}" />
                            </StackPanel>
                        </Button>
                        <!--  Card: 車輛貸款  -->
                        <Button
                            Margin="4,0,4,8"
                            Padding="14,16"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Background="{DynamicResource AppBackground}"
                            BorderBrush="{DynamicResource AppBorder}"
                            BorderThickness="1"
                            Command="{Binding AddAssetDialog.SelectLiabilityTypeCommand}"
                            CommandParameter="loan:車貸"
                            Style="{StaticResource BtnGhost}">
                            <StackPanel HorizontalAlignment="Left">
                                <TextBlock
                                    Margin="0,0,0,6"
                                    FontSize="{StaticResource FontSize2xl}"
                                    Text="&#x1F697;" />
                                <TextBlock
                                    FontSize="{StaticResource FontSizeBase}"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource AppTextPrimary}"
                                    Text="{DynamicResource Portfolio.Liability.Preset.CarLoan}" />
                            </StackPanel>
                        </Button>
                        <!--  Card: 助學貸款  -->
                        <Button
                            Margin="4,0,0,8"
                            Padding="14,16"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Background="{DynamicResource AppBackground}"
                            BorderBrush="{DynamicResource AppBorder}"
                            BorderThickness="1"
                            Command="{Binding AddAssetDialog.SelectLiabilityTypeCommand}"
                            CommandParameter="loan:學貸"
                            Style="{StaticResource BtnGhost}">
                            <StackPanel HorizontalAlignment="Left">
                                <TextBlock
                                    Margin="0,0,0,6"
                                    FontSize="{StaticResource FontSize2xl}"
                                    Text="&#x1F393;" />
                                <TextBlock
                                    FontSize="{StaticResource FontSizeBase}"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource AppTextPrimary}"
                                    Text="{DynamicResource Portfolio.Liability.Preset.StudentLoan}" />
                            </StackPanel>
                        </Button>
                        <!--  Card: 信用貸款  -->
                        <Button
                            Margin="0,0,4,0"
                            Padding="14,16"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Background="{DynamicResource AppBackground}"
                            BorderBrush="{DynamicResource AppBorder}"
                            BorderThickness="1"
                            Command="{Binding AddAssetDialog.SelectLiabilityTypeCommand}"
                            CommandParameter="loan:信貸"
                            Style="{StaticResource BtnGhost}">
                            <StackPanel HorizontalAlignment="Left">
                                <TextBlock
                                    Margin="0,0,0,6"
                                    FontSize="{StaticResource FontSize2xl}"
                                    Text="&#x1F4B0;" />
                                <TextBlock
                                    FontSize="{StaticResource FontSizeBase}"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource AppTextPrimary}"
                                    Text="{DynamicResource Portfolio.Liability.Preset.PersonalLoan}" />
                            </StackPanel>
                        </Button>
                        <!--  Card: 親友借款  -->
                        <Button
                            Margin="4,0,4,0"
                            Padding="14,16"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Background="{DynamicResource AppBackground}"
                            BorderBrush="{DynamicResource AppBorder}"
                            BorderThickness="1"
                            Command="{Binding AddAssetDialog.SelectLiabilityTypeCommand}"
                            CommandParameter="loan:親友借款"
                            Style="{StaticResource BtnGhost}">
                            <StackPanel HorizontalAlignment="Left">
                                <TextBlock
                                    Margin="0,0,0,6"
                                    FontSize="{StaticResource FontSize2xl}"
                                    Text="&#x1F465;" />
                                <TextBlock
                                    FontSize="{StaticResource FontSizeBase}"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource AppTextPrimary}"
                                    Text="{DynamicResource Portfolio.Liability.Preset.FriendLoan}" />
                            </StackPanel>
                        </Button>
                        <!--  Card: 自訂  -->
                        <Button
                            Margin="4,0,0,0"
                            Padding="14,16"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Background="{DynamicResource AppBackground}"
                            BorderBrush="{DynamicResource AppBorder}"
                            BorderThickness="1"
                            Command="{Binding AddAssetDialog.SelectLiabilityTypeCommand}"
                            CommandParameter="loan"
                            Style="{StaticResource BtnGhost}">
                            <StackPanel HorizontalAlignment="Left">
                                <TextBlock
                                    Margin="0,0,0,6"
                                    FontSize="{StaticResource FontSize2xl}"
                                    Text="&#x2795;" />
                                <TextBlock
                                    FontSize="{StaticResource FontSizeBase}"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource AppTextPrimary}"
                                    Text="{DynamicResource Portfolio.Liability.Preset.Custom}" />
                            </StackPanel>
                        </Button>
                    </UniformGrid>
                    <!--  Account preset picker (mode=account)  -->
                    <UniformGrid
                        Grid.Row="1"
                        Columns="3"
                        Visibility="{Binding AddAssetDialog.IsAccountDialogMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Button
                            Margin="0,0,4,8"
                            Padding="14,16"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Background="{DynamicResource AppBackground}"
                            BorderBrush="{DynamicResource AppBorder}"
                            BorderThickness="1"
                            Command="{Binding AddAssetDialog.SelectLiabilityTypeCommand}"
                            CommandParameter="cash:現金"
                            Style="{StaticResource BtnGhost}">
                            <StackPanel HorizontalAlignment="Left">
                                <TextBlock
                                    Margin="0,0,0,6"
                                    FontSize="{StaticResource FontSize2xl}"
                                    Text="&#x1F4B5;" />
                                <TextBlock
                                    FontSize="{StaticResource FontSizeBase}"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource AppTextPrimary}"
                                    Text="{DynamicResource Portfolio.Account.Preset.Cash}" />
                            </StackPanel>
                        </Button>
                        <Button
                            Margin="4,0,4,8"
                            Padding="14,16"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Background="{DynamicResource AppBackground}"
                            BorderBrush="{DynamicResource AppBorder}"
                            BorderThickness="1"
                            Command="{Binding AddAssetDialog.SelectLiabilityTypeCommand}"
                            CommandParameter="cash:銀行活存"
                            Style="{StaticResource BtnGhost}">
                            <StackPanel HorizontalAlignment="Left">
                                <TextBlock
                                    Margin="0,0,0,6"
                                    FontSize="{StaticResource FontSize2xl}"
                                    Text="&#x1F3E6;" />
                                <TextBlock
                                    FontSize="{StaticResource FontSizeBase}"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource AppTextPrimary}"
                                    Text="{DynamicResource Portfolio.Account.Preset.Bank}" />
                            </StackPanel>
                        </Button>
                        <Button
                            Margin="4,0,0,8"
                            Padding="14,16"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Background="{DynamicResource AppBackground}"
                            BorderBrush="{DynamicResource AppBorder}"
                            BorderThickness="1"
                            Command="{Binding AddAssetDialog.SelectLiabilityTypeCommand}"
                            CommandParameter="cash:數位活存"
                            Style="{StaticResource BtnGhost}">
                            <StackPanel HorizontalAlignment="Left">
                                <TextBlock
                                    Margin="0,0,0,6"
                                    FontSize="{StaticResource FontSize2xl}"
                                    Text="&#x1F4F1;" />
                                <TextBlock
                                    FontSize="{StaticResource FontSizeBase}"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource AppTextPrimary}"
                                    Text="{DynamicResource Portfolio.Account.Preset.Digital}" />
                            </StackPanel>
                        </Button>
                        <Button
                            Margin="0,0,4,8"
                            Padding="14,16"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Background="{DynamicResource AppBackground}"
                            BorderBrush="{DynamicResource AppBorder}"
                            BorderThickness="1"
                            Command="{Binding AddAssetDialog.SelectLiabilityTypeCommand}"
                            CommandParameter="cash:定存"
                            Style="{StaticResource BtnGhost}">
                            <StackPanel HorizontalAlignment="Left">
                                <TextBlock
                                    Margin="0,0,0,6"
                                    FontSize="{StaticResource FontSize2xl}"
                                    Text="&#x1F512;" />
                                <TextBlock
                                    FontSize="{StaticResource FontSizeBase}"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource AppTextPrimary}"
                                    Text="{DynamicResource Portfolio.Account.Preset.TimeDeposit}" />
                            </StackPanel>
                        </Button>
                        <Button
                            Margin="4,0,4,8"
                            Padding="14,16"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Background="{DynamicResource AppBackground}"
                            BorderBrush="{DynamicResource AppBorder}"
                            BorderThickness="1"
                            Command="{Binding AddAssetDialog.SelectLiabilityTypeCommand}"
                            CommandParameter="cash:外幣活存"
                            Style="{StaticResource BtnGhost}">
                            <StackPanel HorizontalAlignment="Left">
                                <TextBlock
                                    Margin="0,0,0,6"
                                    FontSize="{StaticResource FontSize2xl}"
                                    Text="&#x1F4B1;" />
                                <TextBlock
                                    FontSize="{StaticResource FontSizeBase}"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource AppTextPrimary}"
                                    Text="{DynamicResource Portfolio.Account.Preset.Foreign}" />
                            </StackPanel>
                        </Button>
                        <Button
                            Margin="4,0,0,8"
                            Padding="14,16"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Background="{DynamicResource AppBackground}"
                            BorderBrush="{DynamicResource AppBorder}"
                            BorderThickness="1"
                            Command="{Binding AddAssetDialog.SelectLiabilityTypeCommand}"
                            CommandParameter="cash:交割戶"
                            Style="{StaticResource BtnGhost}">
                            <StackPanel HorizontalAlignment="Left">
                                <TextBlock
                                    Margin="0,0,0,6"
                                    FontSize="{StaticResource FontSize2xl}"
                                    Text="&#x1F4C8;" />
                                <TextBlock
                                    FontSize="{StaticResource FontSizeBase}"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource AppTextPrimary}"
                                    Text="{DynamicResource Portfolio.Account.Preset.Brokerage}" />
                            </StackPanel>
                        </Button>
                        <Button
                            Margin="0,0,4,0"
                            Padding="14,16"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Background="{DynamicResource AppBackground}"
                            BorderBrush="{DynamicResource AppBorder}"
                            BorderThickness="1"
                            Command="{Binding AddAssetDialog.SelectLiabilityTypeCommand}"
                            CommandParameter="cash:電子支付"
                            Style="{StaticResource BtnGhost}">
                            <StackPanel HorizontalAlignment="Left">
                                <TextBlock
                                    Margin="0,0,0,6"
                                    FontSize="{StaticResource FontSize2xl}"
                                    Text="&#x1F4F2;" />
                                <TextBlock
                                    FontSize="{StaticResource FontSizeBase}"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource AppTextPrimary}"
                                    Text="{DynamicResource Portfolio.Account.Preset.EWallet}" />
                            </StackPanel>
                        </Button>
                        <Button
                            Margin="4,0,4,0"
                            Padding="14,16"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Background="{DynamicResource AppBackground}"
                            BorderBrush="{DynamicResource AppBorder}"
                            BorderThickness="1"
                            Command="{Binding AddAssetDialog.SelectLiabilityTypeCommand}"
                            CommandParameter="cash:儲值卡"
                            Style="{StaticResource BtnGhost}">
                            <StackPanel HorizontalAlignment="Left">
                                <TextBlock
                                    Margin="0,0,0,6"
                                    FontSize="{StaticResource FontSize2xl}"
                                    Text="&#x1F381;" />
                                <TextBlock
                                    FontSize="{StaticResource FontSizeBase}"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource AppTextPrimary}"
                                    Text="{DynamicResource Portfolio.Account.Preset.PrepaidCard}" />
                            </StackPanel>
                        </Button>
                        <Button
                            Margin="4,0,0,0"
                            Padding="14,16"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Background="{DynamicResource AppBackground}"
                            BorderBrush="{DynamicResource AppBorder}"
                            BorderThickness="1"
                            Command="{Binding AddAssetDialog.SelectLiabilityTypeCommand}"
                            CommandParameter="cash"
                            Style="{StaticResource BtnGhost}">
                            <StackPanel HorizontalAlignment="Left">
                                <TextBlock
                                    Margin="0,0,0,6"
                                    FontSize="{StaticResource FontSize2xl}"
                                    Text="&#x2795;" />
                                <TextBlock
                                    FontSize="{StaticResource FontSizeBase}"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource AppTextPrimary}"
                                    Text="{DynamicResource Portfolio.Account.Preset.Custom}" />
                            </StackPanel>
                        </Button>
                    </UniformGrid>
                </Grid>

                <!--  Fields area (scrollable) — Step 2 / form step  -->
                <ScrollViewer
                    Grid.Row="2"
                    Padding="20,16"
                    VerticalScrollBarVisibility="Auto"
                    Visibility="{Binding AddAssetDialog.IsFormStep, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid>
                        <assetSections:AccountCreateSection DataContext="{Binding AddAssetDialog}">
                            <assetSections:AccountCreateSection.Visibility>
                                <Binding
                                    Converter="{StaticResource BooleanToVisibilityConverter}"
                                    ElementName="Root"
                                    Path="DataContext.AddAssetDialog.AddTypeIsAccount" />
                            </assetSections:AccountCreateSection.Visibility>
                        </assetSections:AccountCreateSection>

                        <assetSections:LoanCreateSection DataContext="{Binding AddAssetDialog}">
                            <assetSections:LoanCreateSection.Visibility>
                                <Binding
                                    Converter="{StaticResource BooleanToVisibilityConverter}"
                                    ElementName="Root"
                                    Path="DataContext.AddAssetDialog.AddTypeIsLoan" />
                            </assetSections:LoanCreateSection.Visibility>
                        </assetSections:LoanCreateSection>

                        <assetSections:CreditCardCreateSection DataContext="{Binding AddAssetDialog}">
                            <assetSections:CreditCardCreateSection.Visibility>
                                <Binding
                                    Converter="{StaticResource BooleanToVisibilityConverter}"
                                    ElementName="Root"
                                    Path="DataContext.AddAssetDialog.AddTypeIsCreditCard" />
                            </assetSections:CreditCardCreateSection.Visibility>
                        </assetSections:CreditCardCreateSection>
                    </Grid>
                </ScrollViewer>

                <!--  Error (form step only)  -->
                <TextBlock
                    Grid.Row="3"
                    Margin="20,8,20,0"
                    FontSize="{StaticResource FontSizeXs}"
                    Foreground="{DynamicResource AppDown}"
                    Text="{Binding AddAssetDialog.AddError}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding AddAssetDialog.IsFormStep}" Value="True" />
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="{Binding AddAssetDialog.AddError, Converter={StaticResource StringToVisibilityConverter}}" />
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!--  Footer (form step only)  -->
                <Border
                    Grid.Row="4"
                    Padding="20,12"
                    BorderBrush="{DynamicResource AppBorder}"
                    BorderThickness="0,1,0,0"
                    Visibility="{Binding AddAssetDialog.IsFormStep, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                        <Button
                            Height="30"
                            Margin="0,0,8,0"
                            Padding="14,0"
                            Command="{Binding AddAssetDialog.CloseAddDialogCommand}"
                            Style="{StaticResource BtnGhost}">
                            <TextBlock Foreground="{DynamicResource AppTextSecondary}" Text="{DynamicResource Common.Cancel}" />
                        </Button>
                        <Button
                            Height="30"
                            Padding="14,0"
                            Command="{Binding AddAssetDialog.ConfirmAddCommand}"
                            Style="{StaticResource BtnPrimary}">
                            <TextBlock Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{DynamicResource Common.Confirm}" />
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
